# image_depth optimization matrix

| candidate family | row source policy / layout | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| contiguous depth meters RVV | wrapper-backed contiguous `uint16_t` image | output `float`, tight/padded rows | production-shaped `fillDepthImage` helper | `run_test_compare` passed | `depth_full_640x480` median 1.38x；`depth_full_padded_640x480` median 1.21x | Milkv-Jupiter repeated 5-run positive | `dump_bench_rvv` shows `vle16` / `vfcvt` / `vfmul` / masked `vse32` | Errors=0, Warnings=0 for depth cases | superseded by production adopted | 当前 production row 已采纳 depth contiguous；本 diagnostic row 保留历史候选来源。 |
| contiguous disparity RVV | wrapper-backed contiguous `uint16_t` image | output `float`, tight rows | production-shaped `fillDisparityImage` helper | `run_test_compare` passed | `disparity_full_640x480` median 1.93x | Milkv-Jupiter repeated 5-run positive with long-tail warning | `dump_bench_rvv` shows `vfrdiv` and mask/store RVV instructions | Errors=0, Warnings=1 (`long_tail_or_variance`) | superseded by production adopted | 当前 production row 已采纳 disparity contiguous；本 diagnostic row 保留历史候选来源。 |
| downsample depth `vlse16` RVV | `xStep > 1` nearest-neighbor source | output `float`, tight row | production-shaped `fillDepthImage` downsample helper | `run_test_compare` passed；Std build keeps scalar, RVV build selects downsample RVV | `depth_downsample_640x480_to_320x240` median 1.17x, min 1.03x, max 1.24x | Milkv-Jupiter repeated 5-run positive with long-tail warning | `dump_bench_rvv` shows `vlse16` / `vfmul` / masked `vse32` | Errors=0, Warnings=1 (`long_tail_or_variance`) | superseded by production rejection | production-public row 已拒绝 depth downsample RVV 并保持标量 fallback。 |
| downsample disparity `vlse16` RVV | `xStep > 1` nearest-neighbor source | output `float`, tight row | production-shaped `fillDisparityImage` downsample helper | `run_test_compare` passed；Std build keeps scalar, RVV build selects downsample RVV | `disparity_downsample_640x480_to_320x240` median 1.38x, min 1.13x, max 1.66x | Milkv-Jupiter repeated 5-run positive with long-tail warning | `dump_bench_rvv` shows `vlse16` / `vfrdiv` / masked `vse32` | Errors=0, Warnings=1 (`long_tail_or_variance`) | superseded by production adopted | 当前 production row 已采纳 disparity downsample；本 diagnostic row 保留历史候选来源。 |
| `fillDepthImageRaw()` RVV | raw `uint16_t` output | output `uint16_t`, full-size tight row often `memcpy` | production raw helper | not run in phase 030 | not run; source audit only | not_applicable in current phase | not_applicable | not_run | profile-gated deferred | production probe 完成或 profile 指向非 memcpy raw conversion loop 后再恢复 |
| OpenNI legacy depth/disparity parity | OpenNI depth metadata wrapper | output `float` / `uint16_t` | `openni_camera/openni_depth_image.cpp` legacy production file | not run in phase 030 | not run; source audit only | not_applicable in current phase | not_applicable | not_run | deferred | 另开 legacy parity topic；不作为当前 topic 默认动作 |
| topic-local doc suite | documentation structure | README / testing / evidence / code map | `test-rvv/io/image_depth` docs | not_applicable | not_applicable | not_applicable | not_applicable | not_applicable | adopted | 无；S11 closeout 已完成 |
| production contiguous depth RVV | wrapper-backed contiguous `uint16_t` image | output `float`, tight/padded rows | `DepthImage::fillDepthImage()` public entry | `run_test_compare` Std/RVV 各 9 TEST passed；RVV path hit | `prod_depth_full_640x480` median 1.42x；padded median 1.20x | Milkv-Jupiter production-public 5-run positive | `dump_bench_rvv` production-linked helper / public inline boundary shows `vle16` / `vfcvt` / `vfmul` / masked `vse32` | Errors=0 | adopted | 无；已写入 `doc-rvv/io/image_depth-RVV.zh.md` |
| production contiguous disparity RVV | wrapper-backed contiguous `uint16_t` image | output `float`, tight rows | `DepthImage::fillDisparityImage()` public entry | `run_test_compare` Std/RVV 各 9 TEST passed；RVV path hit | `prod_disparity_full_640x480` median 1.80x, min 1.46x, max 1.82x | Milkv-Jupiter production-public 5-run positive with variance warning | production-linked `vle16` / `vfrdiv` / masked `vse32` | Errors=0, Warnings=1 | adopted with variance warning | 可选 20-run 稳定性增强；不是当前采纳前置条件 |
| production downsample depth RVV | `xStep > 1` nearest-neighbor source | output `float`, tight row | `DepthImage::fillDepthImage()` public entry | `run_test_compare` Std/RVV 各 9 TEST passed；RVV build verifies scalar fallback | `prod_depth_downsample_640x480_to_320x240` context median 0.99x, min 0.98x, max 1.08x | Milkv-Jupiter production fallback coverage | not_applicable for current production RVV | Errors=0；manifest role=`production-fallback-coverage` | rejected for current production patch；scalar fallback kept | 默认不继续；若用户重开需新 phase 设计替代方案 |
| production downsample disparity `vlse16` RVV | `xStep > 1` nearest-neighbor source | output `float`, tight row | `DepthImage::fillDisparityImage()` public entry | `run_test_compare` Std/RVV 各 9 TEST passed；RVV path hit | `prod_disparity_downsample_640x480_to_320x240` median 1.34x, min 1.13x, max 1.39x | Milkv-Jupiter production-public 5-run positive with variance warning | production-linked `vlse16` / `vfrdiv` / masked `vse32` | Errors=0, Warnings=1 (`long_tail_or_variance`) | adopted with variance warning | 可选 20-run 稳定性增强；不是当前采纳前置条件 |
