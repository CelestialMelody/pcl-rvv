# lzf_image_io 优化矩阵

| candidate family | row source policy / layout | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `depth_xyz_rvv` | contiguous depth16 plane -> AoS cloud | test POD `PointXYZRGB` / float x/y/z | `LZFDepth16ImageReader` post-decompress helper | `make run_test_compare` pass | `run_board_lzf_repeated` case `depth_xyz_640x480` | Milkv-Jupiter 5-run mean 1.0389x、median 1.0417x | `dump_bench_rvv` 可见 `vle16.v`、`vzext.vf2`、`vfmul`、`vsse32.v` | Suggestion: `near_threshold_ba` | `deferred-diagnostic` | 暂缓；需要更强 depth code shape 或 production profile |
| `yuv422_planar_rgb_rvv` | planar U/Y/V -> AoS RGB fields | production `PointT` standard-layout + `std::uint8_t r/g/b` gate；其它类型 fallback | `LZFYUV422ImageReader::read/readOMP` post-decompress helper | `make run_test_compare` pass；board gtest pass | `run_board_lzf_yuv422_production_repeated` case `yuv422_planar_rgb_production_640x480` | Milkv-Jupiter production-detail 5-run mean 1.0864x、median 1.0832x | `dump_bench_rvv` 可见 `vlse8.v`、`vsra.vi`、`vsse8.v` | production Doctor `Errors=0, Warnings=0, Suggestions=0` | `adopted weak-positive production behavior` | S11 完成；正式文档 `doc-rvv/io/lzf_image_io-RVV.zh.md` 已创建 |
| `rgb_buffer_to_cloud_rvv` | RGBRGB buffer -> AoS RGB fields | test POD `PointXYZRGB` / byte r/g/b | `LZFBayer8ImageReader` post-debayer copy and RGB24-like copy | `make run_test_compare` pass | `run_board_lzf_repeated` case `rgb_buffer_to_cloud_640x480` | Milkv-Jupiter 5-run mean 0.9902x、median 0.9894x，5/5 < 1 | `dump_bench_rvv` 可见 `vlseg3e8.v`、`vsse8.v` | Error: `ba_degradation_frequency`；Warning: `group_outlier` | `rejected-diagnostic-candidate` | 无；除非另开新写回策略或 profile 指向 RGB copy |
