# OpenNI2 Grabber RVV Optimization Matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| depth_xyz_contiguous_rvv | contiguous frame pixels | `PointXYZ`, `float`, AoS point cloud | production detail helper in `io/src/openni2_grabber.cpp`，public entry dispatch patched | `run_test_compare` passed；production-detail hook bitwise passed | QEMU smoke passed；board production-detail repeated median 1.19x | positive：1.15x-1.22x，checksum matched | production-detail asm target shows `vle16` / `vfcvt` / `vfmul` / `vmseq` / `vsse32` | production-detail Doctor 0/0/0；registry fresh after doc refs | adopted / completed | Adopted；public-entry OpenNI2 smoke 作为外部依赖残余风险。 |
| rgb_overlay_contiguous_rvv | contiguous RGB pixels | `PointXYZRGBA` / `PointXYZRGB`, packed rgba | production-shaped diagnostic helper | RGB alpha/pack tests passed；Phase 030 `PointXYZRGB` / `PointXYZRGBA` bitwise passed | board repeated median 1.02x | neutral / near-threshold | RVV RGB helper asm shows `vlseg3e8` / `vsse32` | near-threshold suggestion + metadata warnings | attempted / deferred | 泛型诊断覆盖已补；不接 production，仅在 profile 或新候选出现后恢复。 |
| ir_intensity_contiguous_rvv | contiguous IR pixels | `PointXYZI`, `float`, AoS point cloud | production-shaped diagnostic helper | IR intensity tests passed | current candidate is scalar for IR | neutral / unstable：median 1.01x，2/5 below 1 | not_applicable for IR RVV candidate | Doctor Error: degradation frequency | deferred | 先做 focused IR RVV candidate，当前不进 production。 |
| mismatch_stride_mapping | depth/image mismatch organized mapping | concrete PCL point structs | production-shaped diagnostic helper | mismatch boundary tests passed | board repeated median 0.98x | neutral / unstable：3/5 below 1 | depth strided-store asm exists | Doctor Error: degradation frequency | attempted / deferred | 不接 production；需要真实 workload profile 或新 stride strategy。 |
