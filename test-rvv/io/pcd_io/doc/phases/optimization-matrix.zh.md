# pcd_io 优化矩阵

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 4-byte field pack/unpack RVV stride path | PCLPointCloud2 contiguous rows | 4 字节字段；AoS `cloud.data` 与 field-major compressed buffer | test-only component ablation for `PCDReader::readBodyBinary` / `PCDWriter::writeBinaryCompressed` layout loops | `run_test_compare` pass；Std/RVV 各 5 个 gtest 通过 | `bench_pcd_io` component cases pass | `component_ablation_repeat_5`：pack positive，unpack positive / weak-positive | `dump_bench_rvv` pass；`vlse32.v` / `vse32.v` / `vle32.v` / `vsse32.v` | Errors=0，Warnings=0，Suggestions=9 | attempted / diagnostic-positive | writer 已升级到 phase 010；reader shaped 已在 phase 040 关闭为 weak-positive |
| production-shaped writer diagnostic | PCLPointCloud2 compressed writer | common 4-byte field PCD layout | `writeBinaryCompressed` compressed payload shaped context | `run_test_compare` pass；payload bytes match scalar | `writer_payload_*_307k` | `writer_payload_repeat_5`：median 1.18x / 1.12x | `dump_bench_rvv` pass；writer payload pack path 可见 `vlse32.v` / `vse32.v` | Errors=0，Warnings=0，Suggestions=4 | adopted upstream via Phase 050 production-public evidence | none |
| writer production RVV pack | PCLPointCloud2 compressed writer | 4 字节有效字段；offset 和 `point_step` 4 字节对齐；`_` padding filtered | `PCDWriter::writeBinaryCompressed(std::ostream&, ...)` public overload | `run_test_compare` pass；Std/RVV 各 10 个 gtest，通过 public payload、mixed size、unaligned point_step、unaligned offset 和 padding fallback | `production_writer_*_307k` | `production_writer_repeat_5`：median 1.33x / 1.25x | public writer path 可见 `vlse32.v` / `vse32.v`，随后进入 `pcl::lzfCompress` | Errors=0，Warnings=0，Suggestions=4 | adopted production behavior | none inside current authorized boundary |
| topic-local doc suite structure | documentation / recovery | README、testing overview、correctness、bench/evidence、optimization evidence、code map、production topic doc | current topic docs | artifact tracking / style checks | not_applicable | not_applicable | not_applicable | registry freshness checked through Phase 050 | adopted / structure complete | none inside current authorized boundary |
| reader shaped RVV unpack + scalar finite scan | PCLPointCloud2 compressed reader payload | 3 个 `FLOAT32` finite 字段 + 1 个 payload 字段；continuous / padded `xyzi` | LZF 解压、field-major unpack、标量 finite scan | `run_test_compare` pass；payload bytes 和 dense flag match scalar | `reader_payload_*_307k` | `reader_payload_repeat_5`：median 1.07x / 1.03x | `dump_bench_rvv` pass；reader payload unpack path 可见 `vle32.v` / `vsse32.v` | Errors=0，Warnings=0，Suggestions=5 | attempted / weak-positive | 不进入 reader production probe；只保留为诊断线索 |
| finite scan RVV mask | PCLPointCloud2 dense scan | `FLOAT32/FLOAT64` finite scan | `readBodyBinary` 后处理 | not_started | not_started | not_started | not_started | not_started | deferred / lower priority | 只有用户明确继续 reader 方向或 profile 指向 finite scan 时另建 phase |
