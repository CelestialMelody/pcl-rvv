# image_yuv422 Optimization Matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| rgb-segment-store-u8 | image row contiguous | `std::uint8_t` YUYV input to RGB output | `ImageYUV422::fillRGB` full-size, even width | pass `run_test_compare`；production direct/fallback 6 tests | `run_board_yuv422_prod_rgb_full` repeated 5-run | production public mean `2.0297x`，median `2.0247x`，min `2.0102x`，max `2.0576x`；checksum 一致 | pass `dump_bench_rvv`；`ImageYUV422::fillRGB` 符号内可见 `vssseg3e8.v` | production public strict A/B：`Errors=0, Warnings=0, Suggestions=0` | adopted | S11 closeout；不外推 downsample、灰度或 OpenNI legacy。 |
| rgb-segment-store-u8 | image row contiguous with padded output | `std::uint8_t` YUYV input to padded RGB output | `ImageYUV422::fillRGB` full-size, even width, `rgb_line_step > width * 3` | pass `run_test_compare`；production direct padding test | `prod_rgb_full_padded_640x480` repeated 5-run | production public mean `2.1030x`，median `2.1012x`，min `2.0889x`，max `2.1169x`；checksum 一致 | pass `dump_bench_rvv`；同一 production symbol | production public strict A/B：`Errors=0, Warnings=0, Suggestions=0` | adopted_evidence_strengthened | padded output boundary closed；不需要 production code change。 |
| contiguous-rgb-strided-u8 | image row contiguous | `std::uint8_t` YUYV input to RGB output | `ImageYUV422::fillRGB` full-size, even width | pass `run_test_compare` | historical Phase 000 smoke | historical weak-positive 约 `1.06x` | pass，旧形态可见 `vsse8.v` | historical all-case doctor 有 downsample fallback Error | superseded | 被 `rgb-segment-store-u8` 替代。 |
| grayscale-strided-y-copy | image row contiguous | `std::uint8_t` YUYV input to gray output | `ImageYUV422::fillGrayscale` full-size | pass scalar fallback correctness | historical smoke only | 不作为 active RVV candidate | not_applicable after candidate removal | not_applicable | rejected | 灰度保持标量 fallback。 |
| downsample-rgb-stride-vsseg3-u8 | image row strided sampling | `std::uint8_t` sampled YUYV to RGB output | test helper `fillRgbCandidate` RGB downsample 640x480 -> 320x240 | pass `run_test_compare`；downsample padding 对拍 | `rgb_downsample_640x480_to_320x240` repeated 5-run | production-shaped diagnostic mean `1.1821x`，median `1.1477x`，min `1.0949x`，max `1.3051x`；checksum 一致 | pass `dump_bench_rvv`；`fillRgbDownsampleRVV` 符号内可见 `vlse8.v` / `vsseg3e8.v` | `Errors=0, Warnings=1, Suggestions=0`，warning 为 long-tail / variance | partial-production-candidate | 进入 `040-downsample-production-integration`，补 production direct 证据。 |
| downsample-rgb-stride-vsseg3-u8 | image row strided sampling | `std::uint8_t` sampled YUYV to RGB output | `ImageYUV422::fillRGB` RGB downsample 640x480 -> 320x240 | pass `run_test_compare`；production direct/fallback 6 tests | `prod_rgb_downsample_640x480_to_320x240` repeated 5-run | production public mean `3.8150x`，median `3.9294x`，min `3.3750x`，max `3.9692x`；checksum 一致 | pass `dump_bench_rvv`；production symbol 内可见 RVV 计算和 segmented store | production public strict A/B：`Errors=0, Warnings=1, Suggestions=0`，warning 为 long-tail / variance | adopted | S11 closeout；不外推灰度、其它 resize ratio 或 OpenNI legacy。 |
| grayscale-downsample-rvv | image row strided sampling | `std::uint8_t` sampled YUYV to gray output | grayscale downsample | pass scalar fallback correctness | not_applicable | not_applicable | not_applicable | not_applicable | rejected | 灰度保持标量 fallback。 |
