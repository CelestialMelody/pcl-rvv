# pcd_io_templated_writer 优化矩阵

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 4-byte field compressed writer RVV stride path | contiguous PointCloud rows | 4 字节字段；PointCloud AoS 到 field-major buffer | test-only component ablation for `PCDWriter::writeBinaryCompressed<PointT>` layout loop | `run_test_compare` pass；board test pass | `bench_pcdtw` pack-only cases | `1.4892x / 1.4466x / 1.5739x` mean speedup；5 runs | `vlse32.v` / `vse32.v` in `bench_pcdtw_rvv.asm` | Errors=0 Warnings=0 Suggestions=0 | attempted positive | 进入 phase 010 shaped diagnostic |
| binary writer packed output path | contiguous PointCloud rows | 4 字节字段；point-major AoS 到 point-major packed output，覆盖 no-padding / tail-padding | `writeBinary<PointT>` packed output loop 的 test-only component ablation | `run_test_compare` pass；mixed field fallback pass | `bench_pcdtw --case-filter binary_*` | median `1.2738x / 1.1842x / 6.8125x`；5 runs；small case 单独 outlier-scoped | `vlse32.v` / `vsse32.v` in `bench_pcdtw_rvv.asm` | Errors=0 Warnings=1 Suggestions=0；small case group_outlier | attempted positive / binary-component-candidate | Phase 050 PI1 已完成；不自动修改 production |
| ASCII writer | contiguous or indexed PointCloud rows | datatype switch + stream output | `writeASCII<PointT>` | not_started | not_started | not_started | not_started | not_started | rejected with evidence | 需要 profile 才恢复 |
| production-shaped compressed writer diagnostic | contiguous PointCloud rows | synthetic PointXYZRGB-like 4 字节字段；`point_step=16/20` | pack + LZF compress shaped context | `run_test_compare` pass；compressed payload 对拍一致 | `bench_pcdtw --case-filter compressed_*` | mean `1.3904x / 1.3526x / 1.3493x`；5 runs | `vlse32.v` / `vse32.v` in `bench_pcdtw_rvv.asm`；LZF scalar | Errors=0 Warnings=0 Suggestions=0 | partial-production-candidate | 创建 PI1 production integration plan |
| compressed writer production public patch | contiguous PointCloud rows | `PointCloud<PointT>` 中所有有效字段均为 4 字节且 offset / stride 对齐；Phase 060 覆盖 compact 16B 和 padding 20B registered point types | `writeBinaryCompressed<PointT>(file_name, cloud)` production boundary | production-direct public writer tests pass；mixed field fallback pass；Std/RVV 8/8 gtest pass | `bench_pcdtw --case-filter production_compressed_*`；production-public board repeated | mean `1.3122x / 1.2495x / 1.1239x`；small 1/5 退化，smoke-only | public writer symbols contain `vlse32.v` / `vse32.v` | Errors=0 Warnings=4 Suggestions=0；warnings explained | adopted production behavior | closed；正式文档 `doc-rvv/io/pcd_io_templated_writer-RVV.zh.md` |
| structure parity doc suite | not_applicable | topic-local docs | README、testing overview、correctness、benchmark/evidence、optimization evidence、code map、phase suite、production topic doc | documentation consistency scan | not_applicable | not_applicable | not_applicable | not_applicable | doc-suite-parity-complete | Phase 070 后保持 role 文档同步 |
| binary writer PI1 production integration plan | contiguous PointCloud rows | `PointCloud<PointT>` 中所有有效字段均为 4 字节且 offset / stride 对齐 | `writeBinary<PointT>(file_name, cloud)` production boundary | planned public entry direct + fallback / indices unchanged tests | planned production public/detail bench | planned, requires production patch and board run | planned production symbol attribution | planned | PI1-plan-complete / phase_deferred + unblocked | 创建 Phase 080 binary writer PI2 production patch |
| binary writer field-outer production public patch | contiguous PointCloud rows | 4 字节字段；compact 16B 和 tail-padding 20B registered point types；indices overload 保持标量 | `writeBinary<PointT>(file_name, cloud)` production boundary | 回滚前 Std/RVV 11/11 gtest pass；回滚后该 field-outer production-direct tests 已移除 | `production_binary_*` 已移除；Phase 080 historical board repeated 保留 | mean `0.9842x / 0.9727x / 0.9810x`；大规模和 small 均无稳定收益 | 回滚前 public writer symbols contained `vlse32.v` / `vsse32.v`；回滚后 field-outer helper 不存在 | Errors=3 Warnings=0 Suggestions=0；`ba_degradation_frequency` blocks adoption | rollback/no-production | closed；由 Phase 090/100 新实现族替代 |
| binary tuple / segment diagnostic | contiguous PointCloud rows | 4 个连续 4-byte fields；compact 16B / padding 20B / small 512 | test-only tuple / segment output helper | Std/RVV 11/11 gtest pass；mixed fallback pass | `binary_tuple_*` QEMU smoke 和 board repeated | diagnostic mean `9.8247x / 4.0905x / 18.0526x`；small smoke-only | bench binary contains `vlse32.v` / `vsseg4e32.v` for padding | Errors=0 Warnings=2 Suggestions=0；padding / small outliers 分开解释 | diagnostic-positive / production-probe-candidate | Phase 100 completed |
| binary tuple / segment production path | contiguous PointCloud rows | 4 个连续 4-byte fields；compact 16B memcpy；padding 20B RVV segment；indices overload 保持标量 | `writeBinary<PointT>(file_name, cloud)` production boundary | Std/RVV 15/15 gtest pass；compact/memcpy hit、padding/RVV hit、mixed fallback、indices unchanged pass | `production_binary_tuple_*` QEMU smoke 和 board repeated | production-public mean `1.3046x / 1.3240x / 1.1082x`；small smoke-only | production bench binary contains `vlse32.v` / `vsseg4e32.v` for padding | Errors=0 Warnings=1 Suggestions=0；small long-tail 降级为 smoke | adopted production behavior | closed；formal doc-rvv refreshed |
