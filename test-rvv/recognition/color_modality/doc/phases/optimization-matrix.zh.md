# color_modality Optimization Matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `cm-filter-3x3-rvv` | organized public input | `PointXYZRGB`, byte quantized map, `float` color math | `ColorModality<PointXYZRGB>::processInputData()` filter stage | `run_test_compare` forced scalar/RVV map compare | `production_process_320x240`, `production_process_641x481_tail` | historical Phase 030 handoff recorded positive, superseded by Phase 040 | `vle8/vse8/vmseq/vmsgtu/vmerge` | superseded by Phase 040 doctor | adopted as part of Phase 040 family | none |
| `cm-rgb-extrema-quantize-rvv` | organized public input | exact `PointXYZRGB`, AoS byte stride, `float` semantics represented by integer-scaled distance comparison | `ColorModality<PointXYZRGB>::processInputData()` quantize stage | `run_test_compare` passed, RVV path hook hit, forced scalar/RVV output equal | same production_process cases | `repeated_phase040_quantize_filter_rvv/summary.md`: median `2.490x` / `2.410x`, `0/5` regressions | `vlse8` byte field loads plus filter RVV byte ops in `bench_cm_rvv.asm` | `Errors=0, Warnings=0, Suggestions=4` | adopted | none inside current evidence boundary |
| `extractFeatures` RVV | organized filtered map + mask | list/sort feature candidate state | `extractFeatures()` | no target | no profile / no ablation | none | none | not run | deferred until profile evidence | profile or component ablation required |
| `computeDistanceMap` RVV | mask map per bin | scalar two-pass dynamic-programming distance map | `computeDistanceMap()` called from `extractFeatures()` | no target | no profile / no ablation | none | none | not run | deferred until profile evidence | profile or component ablation required |
| generic RGB traits expansion | organized public input | other point types with `r/g/b` fields | `ColorModality<PointInT>::processInputData()` | no fallback traits test | no board evidence | none | none | not run | rejected for this closeout scope | new point-type expansion phase only if caller demand appears |
