# Optimization Matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `depth-quantize-rvv` | organized depth image | `PointXYZRGBA` / float / contiguous AoS | `SurfaceNormalModality<PointInT>::processInputData()` 中的 `computeAndQuantizeSurfaceNormals2()` | `run_test_compare` 4/4 | `bench_snm --case-filter production_process_*` | Phase 010 5-run weak-positive，checksum 一致 | `check_snm_production_rvv_asm` | `Errors=0 / Warnings=0 / Suggestions=4` | adopted | closeout |
| `filter-5x5-rvv` | quantized map | byte map | `filterQuantizedSurfaceNormals()`，经真实 `processInputData()` public entry | `run_test_rvv`、`run_test_compare` 通过，RVV build 命中 `FilterRvv` hook，小输入 fallback 标量 | `bench_snm --case-filter production_process_320x240,production_process_641x481_tail` | Phase 020 5-run positive，median `1.420x` / `1.420x`，checksum 一致，`B/A < 1 = 0/5` | `check_snm_production_rvv_asm` 命中 `filterQuantizedSurfaceNormals` 与 `vle8/vse8/vmseq/vmsgtu` | `Errors=0 / Warnings=0 / Suggestions=4` | adopted | closeout / create Phase 030 spread plan |
| `snm-spread-rvv-2pass` | spreaded map | byte map / default spread 8 | surface-normal 专用 `spreadFilteredQuantizedSurfaceNormals()`，经真实 `processInputData()` public entry | `run_test_rvv`、`run_test_compare` 通过，RVV build 命中 `SpreadRvv` hook，非默认 spread size fallback 标量 | `bench_snm --case-filter production_process_320x240,production_process_641x481_tail` | Phase 030 5-run positive，median `1.620x` / `1.640x`，checksum 一致，`B/A < 1 = 0/5` | `check_snm_production_rvv_asm` 命中 spread helper / spread 符号与 `vor/vse8` | `Errors=0 / Warnings=0 / Suggestions=4` | adopted | closeout |
| `generic-quantized-map-spread-rvv` | shared QuantizedMap | byte map / all modality callers | 公共 `QuantizedMap::spreadQuantizedMap()` | not covered for color / color-gradient / color-only callers | not covered | not covered | not covered | not covered | deferred / separate topic | 另开跨 modality topic |
