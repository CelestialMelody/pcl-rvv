# sac_model_sphere 优化矩阵

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| existing count RVV | direct indexed `indices_` | `PointXYZ`, float xyz layout | production `countWithinDistance` | done: `run_test_compare`，public entry 与 reference 一致 | done: public count board bench | 5-run median `3.5192x`，min/max `3.4878x / 3.5435x`，checksum `13484` | done: `countWithinDistanceRVV` 符号级 RVV instruction count（RVV 指令计数）为 `19` | repeated doctor 未对 count 报 Error / Warning | adopted/current production behavior | 若后续改动 production count helper，需要 production direct 重跑；本阶段无未阻塞 count 动作。 |
| test-only select RVV squared-distance + scalar output | direct indexed `indices_` | correctness 覆盖 `PointXYZ` / `PointXYZI`；board 覆盖 `PointXYZ` float xyz layout | historical production-shaped diagnostic for `selectWithinDistance` | done: candidate vs public output | done: candidate rows in board bench | Phase 000 5-run median `1.4216x`，min/max `1.4141x / 1.4369x`，checksum `13484` | historical partial: 诊断候选符号级归属未闭合 | no Error for candidate select | superseded by production direct | Phase 020 已把该候选转为 production `selectWithinDistanceRVV`；历史诊断只作为接入前证据。 |
| test-only getDistances RVV squared-distance + scalar sqrt/store | direct indexed `indices_` | correctness 覆盖 `PointXYZ` / `PointXYZI`；board 覆盖 `PointXYZ` float xyz layout | production-shaped diagnostic for `getDistancesToModel` | done: candidate vs public output | done: candidate rows in board bench | 5-run median `0.7781x`，min/max `0.7605x / 0.7906x`，checksum `13484` | partial: 候选符号计数为 `0`，内联归属不闭合 | Error: 5/5 run 退化 | rejected for current candidate family | 不按当前 scratch + 标量 sqrt 形态进入 production；恢复条件是 RVV sqrt/helper audit 或新的 dense-store 消融。 |
| public selectWithinDistance Phase 020 production RVV | direct indexed `indices_` | `PointXYZ` board performance；`RVVXYZFloatLayout<PointT>` production gate；`PointXYZI` correctness | historical production public entry before `vcompress` | done: `run_test_compare`，public / Standard / RVV helper 对拍 | done: production repeated board | Phase 020 5-run median `1.5020x`，min/max `1.4532x / 1.5129x`，checksum `13484` | done: `selectWithinDistanceRVV` 符号级 RVV instruction count 为 `17` | no Error / Warning on select production row | superseded by Phase 045/046 adopted `vcompress` production patch | Phase 045/046 已用 `vcompress` production direct 证据取代当前工作区实现。 |
| public getDistancesToModel current scalar | direct indexed `indices_` | `PointXYZ` float xyz layout | production public entry without RVV dispatch | done: public entry correctness | done: public getDistances board bench | Phase 020 5-run median `0.9900x`，min/max `0.9807x / 0.9952x` | manifest `rvv_instr_count=0` | Error: 5/5 run 低于 1 | attempted/public scalar stability only | 不作为 RVV 负向结论；当前 getDistances 候选另行 rejected。 |
| production select dispatch with `vcompress` | direct indexed `indices_` | registered single-float xyz layout; built-in representative board performance covers `PointXYZ` / `PointXYZI` / `PointXYZRGB` / `PointXYZRGBA` | production `selectWithinDistance` | done: `run_test_compare`，public / Standard / RVV helper 对拍 | done: Phase 045 `PointXYZ`、Phase 050 `PointXYZI`、Phase 060 RGB/RGBA public select production repeated board | `PointXYZ` median `2.0989x`；`PointXYZI` `1.5901x`；`PointXYZRGB` `1.6225x` clean；`PointXYZRGBA` `1.5528x` with 1/5 degrade warning | done: `selectWithinDistanceRVV` `rvv_instr_count=27`，符号内可见 `vcompress.vm` | select rows clean for `PointXYZ` / `PointXYZI` / `PointXYZRGB`; RGBA has degradation + long-tail Warnings; Doctor Errors belong to out-of-scope getDistances candidate rows | adopted/current production behavior; RGBA adopted with stability warning | 自定义 registered xyz 和其它规模仍未覆盖；需要先定义代表类型 / 输入边界。 |
| production getDistances dispatch | direct indexed `indices_` | registered single-float xyz layout | production `getDistancesToModel` | not_started | not_started | not_started | not_started | not_started | rejected/deferred | 当前 candidate family 为 negative；后续只在 RVV sqrt/helper audit 产生新候选后恢复。 |
| point type expansion | direct indexed `indices_` | `PointXYZI` / `PointXYZRGB` / `PointXYZRGBA` PointXYZ-like layout | production point-type expansion | done: RGB/RGBA correctness and bench log shape | done: Phase 050 `PointXYZI` and Phase 060 RGB/RGBA dedicated board repeated | `PointXYZI` median `1.5901x`；`PointXYZRGB` median `1.6225x` min/max `1.6181x / 1.6331x`；`PointXYZRGBA` median `1.5528x` min/max `0.9184x / 1.6354x` | done: each tested point type `selectWithinDistanceRVV` `rvv_instr_count=27` | RGB select row clean; RGBA select has 1/5 degradation and long-tail Warnings; getDistances Errors out-of-scope | adopted for listed point types; RGBA with stability warning | 自定义 registered xyz 仍未覆盖；无代表类型前不继续扩大。 |
| topic-local doc suite and test support layout | not_applicable | topic-local docs and test support recovery | README / testing / correctness / bench / optimization evidence / code map；`include` / `include/impl` helpers | adopted | adopted | not_applicable | not_applicable | not_applicable | adopted | Phase 010 已补齐文档套件；Phase 055 已迁移 test/bench 支撑到 `include` / `include/impl`。 |
| evidence registry / doctor target alias | not_applicable | topic-local Make target and evidence freshness | repeated Phase 000 / production Phase 020 / production Phase 045 / Phase 050 / Phase 060 manifest / doctor / registry | not_applicable | not_applicable | summary evidence only | not_applicable | adopted: repeated / production / vcompress / point-type status targets | adopted/fresh | Phase 060 已新增 RGB/RGBA 独立 registry target；最终收尾复核全部 status target 均为 fresh。 |
