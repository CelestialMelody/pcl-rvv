# sac_model_circle optimization matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| existing count RVV | direct indexed `indices_` | `PointXYZ` / float x-y layout；`PointXYZI` correctness expansion | production `countWithinDistance` | `run_test_compare`；5-run board RVV gtest passed | `bench_sac_model_circle` public count row | 5-run board median 1.4012x，min 1.3898x，`B/A < 1` 为 0/5 | `countWithinDistanceRVV`，15 条 RVV 指令 | Errors=0，Warnings=0，Suggestions=0 | adopted | none in current scope |
| select RVV gather + scalar exact error writeback | direct indexed `indices_` | `PointXYZ` / float x-y layout；`PointXYZI` correctness expansion | production `selectWithinDistance` historical baseline | RED/GREEN 完成；`run_test_compare`；board smoke 5 轮通过 | `bench_sac_model_circle` public select row | Phase 000 5-run board median 1.6702x，min/max 1.5732x / 1.6922x，`B/A < 1` 为 0/5 | `selectWithinDistanceRVV`，23 条 RVV 指令 | Errors=0，Warnings=0，Suggestions=0 | superseded by Phase 090 full-RVV error tail | 保留为 historical baseline，不再代表当前 production truth。 |
| select full-RVV error tail production | direct indexed `indices_` | `PointXYZ` / float x-y layout；active double error output；`PointXYZI` correctness expansion | production `selectWithinDistance` | `run_test_compare`；board gtest 5 轮各 8/8 通过 | `bench_sac_model_circle` public select row | Phase 090 5-run board median 2.5700x，min/max 2.5357x / 2.6770x，`B/A < 1` 为 0/5 | `selectWithinDistanceRVV`，确认 `vcompress.vm`、`vfsqrt.v`、`vfwcvt.f.f.v`、`vse64.v`；24 条 RVV 指令 | Errors=0，Warnings=0，Suggestions=0 | adopted production behavior | none in current scope |
| getDistances RVV sqr + scalar sqrt/store test-only candidate | direct indexed `indices_` | `PointXYZ` / float x-y layout | RVV binary 内 public `getDistancesToModel` companion vs test-only helper | RED/GREEN 完成；`run_test_compare`；board smoke 5 轮各 1/1 通过 | `bench_sac_model_circle` public/candidate getDistances rows | Phase 020 5-run board B/A median 0.6590x，min/max 0.6528x / 0.6629x，`B/A < 1` 为 5/5 | `getDistancesToModelCandidateRVV`，13 条 RVV 指令；public row 44 条 RVV 指令但不是手写 production RVV | Errors=1（5/5 退化），Warnings=1（指令更少但更慢），Suggestions=0 | rejected with diagnostic evidence；superseded by Phase 050/060 full-RVV family | 当前 helper 形态不进 production probe；不要把该负向外推到 full-RVV 实现族。 |
| getDistances vfsqrt full-RVV test-only candidate | direct indexed `indices_` | `PointXYZ` / float x-y layout / dense double output | RVV binary 内 public companion vs test-only full-RVV helper | `run_test_compare`；`run_circle_getdistances_full_rvv_candidate_test` | `bench_sac_model_circle` full-RVV diagnostic row | Phase 050 5-run B/A median 1.4745x，min/max 1.4700x / 1.4905x，`B/A < 1` 为 0/5 | `getDistancesToModelFullRVV`，确认 `vfsqrt.v`、`vfwcvt.f.f.v`、`vse64.v` | Errors=0，Warnings=0，Suggestions=0 | production probe entered | closed by Phase 060 production direct evidence |
| getDistances vfsqrt full-RVV production | direct indexed `indices_` | `PointXYZ` / float x-y layout / dense double output | production public `getDistancesToModel` Std build vs RVV build | `run_test_compare`；`run_circle_getdistances_production_test`；board gtest filter `PublicGetDistancesMatchesStandardAndDirectRVV` | public getDistances row in Std/RVV build | Phase 060 5-run B/A median 1.4737x，min/max 1.4687x / 1.4962x，`B/A < 1` 为 0/5 | `getDistancesToModelRVV`，确认 `vfsqrt.v`、`vfwcvt.f.f.v`、`vse64.v` | Errors=0，Warnings=0，Suggestions=0 | adopted production behavior by Phase 070 | none in current scope |
| identity-index strided load | direct indexed identity `indices_` | `PointXYZ` / float x-y layout | historical production-detail candidate for select/count | correctness 和 `check_identity_strided_asm` 曾在候选实现存在时通过；当前不作为生产验证 gate | `bench_sac_model_circle` identity mode，gather-only RVV vs identity-strided RVV | Phase 040 5-run board：select median 0.9914x，count median 0.9698x，二者 `B/A < 1` 均为 5/5 | 候选二进制中出现 `vlse32.v`；当前 production 不要求 strided-load asm | Errors=2，Warnings=0，Suggestions=0 | rejected with strict A/B evidence；production reverted to gather-only | none；历史 replay target 需 `ALLOW_HISTORICAL_IDENTITY_PROBE=1`。 |
| structure parity doc suite | not_applicable | topic-local docs / summary-only evidence | README、evaluation、testing overview、correctness、benchmark/evidence、optimization evidence、code map、phase index、production `doc-rvv` | 文档路径和 target 字典已对齐当前 Makefile、test、bench、script | not_applicable | 不新增性能数据 | not_applicable | not_applicable | adopted in Phase 030/040 | none |
