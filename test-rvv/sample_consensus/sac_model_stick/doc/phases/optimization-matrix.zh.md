# sac_model_stick optimization matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| count-indexed-gather-f32m2-dual-count | direct indexed `indices_` | Phase 000: `PointXYZ`, float xyz AoS, `Eigen::VectorXf`, `double threshold` | `countWithinDistance` test-only candidate vs public entry | done: diagnostic correctness | done: board repeated compare, candidate median 4.1767x | done, 5-run budget, positive-stable for candidate | done: `countWithinDistanceCandidateRVV` contains FMA / mask / popcount RVV instructions | done: Errors=0, Warnings=1, Suggestions=1; warning only on public weak cross-check | adopted via Phase 080 production path | no diagnostic action remains |
| select-indexed-gather-f32m2-compress | direct indexed `indices_` | Phase 020: `PointXYZ`, float xyz AoS, `Eigen::VectorXf`, `double threshold` | `selectWithinDistance` test-only candidate vs public entry | done: diagnostic correctness | done: board repeated compare, candidate median 3.4432x | done, 5-run budget, positive-stable for candidate | done: `selectWithinDistanceCandidateRVV` contains FMA / mask / popcount / `vcompress` RVV instructions | done: Errors=0, Warnings=1, Suggestions=1; warning only on public weak cross-check | adopted via Phase 080 production path | no diagnostic action remains |
| getDistances-indexed-gather-f32m2-sqrt-store | direct indexed `indices_` | Phase 040: `PointXYZ`, float xyz AoS, `Eigen::VectorXf`, `radius_max_` penalty | `getDistancesToModel` test-only candidate vs public entry | done: diagnostic correctness | done: board repeated compare, candidate median 2.5553x | done, 5-run budget, positive-stable for candidate; public row was historical negative cross-check | done: `getDistancesToModelCandidateRVV` contains indexed load / FMA / `vfsqrt.v` / store RVV instructions | done: Errors=1, Warnings=0, Suggestions=0; Error only on historical public negative cross-check, candidate row clean | adopted via Phase 080 production path | no diagnostic action remains |
| production-stick-three-entry-rvv | direct indexed `indices_` | traits-gated xyz AoS `PointT`；Phase 080 board performance covers `PointXYZ`, `Eigen::VectorXf`, float xyz AoS | production `countWithinDistance`、`selectWithinDistance`、`getDistancesToModel` | done: `make run_test_compare` Std/RVV 11/11；public production direct tests cover count/select/getDistances and Phase 090 representative point types | done: production public 5-run board | done: count/select/getDistances median 4.1729x / 3.3023x / 2.5883x | done: `countWithinDistanceRVV`、`selectWithinDistanceRVV`、`getDistancesToModelRVV` contain RVV instructions | done: Errors=0, Warnings=0, Suggestions=0 | production-adopted | dedicated point-type performance requires new board phase |
| getDistances-vector-penalty-writeback | direct indexed `indices_` | `PointXYZ` board performance；traits-gated xyz AoS production path；`Eigen::VectorXf`, `radius_max_` penalty | production `getDistancesToModelRVV` implementation shape | done: `run_stick_getdistances_tests` RVV 3/3；`run_test_compare` Std/RVV 11/11 + 11/11 | done: public getDistances 5-run board repeated | done: min / median / max 3.4122x / 3.6879x / 3.7150x | done: source shape has mask / merge / `vfwcvt` / `vse64`; asm has `vfwcvt.f.f.v` and `vse64.v` | done: Errors=0, Warnings=0, Suggestions=0 | production-adopted for current implementation shape | no same-scope action remains; strict old/new RVV A/B only if reviewer requires exact family delta |
| doc-suite-structure | not_applicable | topic-local docs for current production-adopted scope | README / evaluation / phase suite / role docs | done: role docs created and linked | not_applicable: no new bench | not_applicable: no new board run | not_applicable: no new asm | registry freshness checked by evidence status targets | completed | no doc-suite unblocked action remains |
| getDistances-target-granularity | not_applicable | topic-local Makefile target | `run_stick_getdistances_tests` alias | done: RVV build gtest filter covers three getDistances cases | not_applicable: no new bench | not_applicable: no new board run | not_applicable: no new asm | registry freshness checked by evidence status targets | completed | no target-granularity unblocked action remains |
| point-type-expansion | direct indexed `indices_` | `PointXYZI`、`PointXYZRGB`、`PointXYZRGBA`、`PointXYZRGBNormal` | production public entries under current traits gate | done: `run_stick_point_type_tests` RVV 1/1；`run_test_compare` Std/RVV 11/11 | not_applicable: correctness-only phase | not_applicable for performance | production helper symbols unchanged; Phase 080 asm still applies | not_applicable without new board performance manifest | adopted_for_representative_point_type_correctness | dedicated point-type performance requires new board phase |
