# sac_model_plane 优化矩阵

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| indexed xyz gather + shared distance kernel | direct indexed cloud | `PointXYZ` board；`PointXYZI` / `PointXYZRGB` / `PointXYZRGBA` / `PointXYZINormal` correctness；`Scalar=float` coeffs；registered xyz AoS | `selectWithinDistance` / `countWithinDistance` / `getDistancesToModel` public entry | pass: `run_test_compare`，Std/RVV 各 7 个 gtest；board RVV gtest 7/7 | Phase 000 5-run board public Std/RVV | pass: select 3.1965x、count 1.6678x、getDistances 2.3695x | pass: `vluxei32`、`vfmacc`、select `vcompress`、getDistances `vfwcvt` / `vse64` | pass with suggestions: Errors=0, Warnings=0 | adopted | No further performance candidate in current scope |
| identity-index strided load for select/count | identity direct cloud | `PointXYZ` / `Scalar=float` / registered xyz AoS | `selectWithinDistanceRVV`、`countWithinDistanceRVV` | pass: `CloudOnlyIdentityIndicesMatchStandardPath` included in `run_test_compare` | Phase 010 identity/shuffled board, `BENCH_ARGS='65536 200 identity|shuffled'` | identity median: select 3.3896x、count 2.1969x；shuffled median: select 3.1895x、count 1.6663x | pass: select/count contain `vid.v` / `vmsne` / `vlsseg3e32` and keep `vluxei32` fallback | pass for adopted entries: Errors=0, Warnings=0, Suggestions=4 for select/count | adopted_narrow | No further identity performance action in current scope |
| identity-index strided load for getDistances | identity direct cloud | `PointXYZ` / `Scalar=float` / registered xyz AoS | `getDistancesToModelRVV` candidate branch | pass during mixed-gate trial, then production code narrowed back to gather | Phase 010 mixed and narrowed reruns | narrowed current median remains positive as gather-only, but identity branch did not beat Phase 000 enough and earlier shuffled mixed gate regressed | current asm has `vluxei32` only; no `vlsseg3e32` in getDistances helper | identity report has Warning=1 for getDistances long tail; not used for adoption | rejected with evidence | Reopen only with RVV-vs-RVV detail A/B or a lower-overhead identity detection design |
| unsupported layout fallback | direct indexed cloud | non-registered or non-single-float xyz | public entry fallback | attempted/rejected test shape: unregistered custom point does not instantiate full model cleanly | not_applicable | not_applicable | compile-time gate review | manual boundary | deferred | Design a registered fallback fixture if reviewer requires runtime evidence |
| point type expansion | direct indexed cloud | `PointXYZI` / `PointXYZRGB` / `PointXYZRGBA` / `PointXYZINormal` | public entry | pass: shuffled and identity public entry vs Standard path | correctness-only; board RVV gtest 7/7 | no dedicated performance conclusion | compile/path evidence in RVV build | not_applicable for correctness-only | adopted_for_correctness | Dedicated board performance only if user wants broader point-type performance evidence |
| explicit empty indices public entry | direct indexed cloud with empty subset | `PointXYZ` / `Scalar=float` / registered xyz AoS | `selectWithinDistance` / `countWithinDistance` / `getDistancesToModel` public entry | pass: `ExplicitEmptyIndicesMatchStandardPath` included in `run_test_compare` | not_applicable | board RVV gtest 7/7 | not_applicable: no new RVV instruction boundary | not_applicable for correctness-only | adopted_for_correctness | No performance action; this closes a correctness coverage gap |
| evidence registry / metadata hardening | current board evidence | current production evidence summaries | evidence freshness | manifest targets exist for Phase 000 and Phase 010 | deferred | manifests generated, raw logs remain local-only | not_applicable | suggestions remain for env metadata and binary hash | turn_stop_deferred with stop_condition_hit: not a performance optimization candidate | Reopen when preparing evidence-log commit or stronger archival evidence |
