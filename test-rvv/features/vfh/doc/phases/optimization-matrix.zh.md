# VFH Optimization Matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `vfh-scalar-reference-scaffold` | full-cloud sequential indices | `PointNormal -> VFHSignature308`, float, dense finite AoS | public compute + test-only reference | `run_test_compare` public reference tests | not_applicable | QEMU / board correctness 已通过 | not_applicable | not_applicable | adopted | 保持为后续 candidate 的 scalar baseline |
| `vfh-centroid-spfh-rvv` | full-cloud sequential indices | same, staged SoA candidate | test-only `computeVFHSignatureCentroidSPFHRVV` | `VFHCandidate.CentroidSPFHRVVMatchesReference` | `candidate_vfh_centroid_spfh_rvv` | Phase 020 5-run board `2.59x-2.66x`，mean `2.628x` | `dump_bench_rvv` 已见 RVV math / reduction | Phase 020 Doctor `0E/0W/10S`，candidate 只有 metadata suggestion | attempted / positive diagnostic | 作为 production probe 的对照 family |
| `vfh-viewpoint-histogram-rvv` | full-cloud sequential indices | same, staged SoA candidate | test-only `computeVFHSignatureSPFHAndViewpointRVV` | `VFHCandidate.SPFHAndViewpointRVVMatchesReference` | `candidate_vfh_spfh_viewpoint_rvv` | Phase 020 5-run board `2.86x-2.91x`，mean `2.884x` | `dump_bench_rvv` 已见 RVV bench binary；production 归属不适用 | Phase 020 Doctor `0E/0W/10S`，candidate 只有 metadata suggestion | attempted / positive diagnostic | 纳入 PI1/PI2 候选优先范围 |
| `vfh-public-dilution-baseline` | public full-cloud input | same | `VFHEstimation::compute()` public entry | reference matches public compute | `public_vfh_compute_baseline` | Phase 020 5-run board `1.00x-1.02x`，mean `1.004x`，近阈值 | no production helper expected | Phase 020 Doctor 对 near-threshold 给 suggestion | attempted / near-threshold diagnostic baseline | 仅作 production gap 参照 |
| `vfh-centroid-normal-reduction-rvv` | full-cloud sequential indices | same, staged SoA candidate | test-only `computeVFHSignatureCentroidsSPFHAndViewpointRVV` | `VFHCandidate.CentroidsSPFHAndViewpointRVVMatchesReference` | `candidate_vfh_centroids_spfh_viewpoint_rvv` | Phase 030 5-run board `2.98x-3.07x`，mean `3.018x` | `dump_bench_rvv` 可见 `vfadd.vv` / `vfredosum.vs` | Phase 030 Doctor `0E/0W/12S`，candidate 只有 metadata suggestion | attempted / positive diagnostic | 纳入 PI2 production probe 优先 family |
| `vfh-production-probe-phase040` | public full-cloud input | `PointInT` xyz float AoS, `PointNT` normal float AoS, `PointOutT=VFHSignature308` | `VFHEstimation::computeFeature()` default public path | `VFHProductionRVV.DefaultPublicBoundaryHelperMatchesPublicCompute` + `HelperRejectsSizeComponentFallbackBoundary` | `production_vfh_compute_default` | Phase 040 mean `1.36195x`，checksum 一致 | production helper 符号可见，`computeFeature()` 调用 RVV helper | Phase 040 Doctor `0E/1W/11S`；Warning 非 production case | attempted / positive production-public / superseded | 被 Phase 050 替代 |
| `vfh-production-chunk-local-staging` | public full-cloud input | same | 同 Phase 040，不扩大 fallback boundary | `run_test_compare` Std/RVV 各 7 个测试 | `production_vfh_compute_default` | Phase 050 mean Std `8.80421 ms`，RVV `6.10923 ms`，speedup `1.44115x` | production helper RVV 指令仍可归属 | Phase 050 Doctor `0E/0W/11S` | attempted / positive production-public / superseded | 被 Phase 060 替代 |
| `vfh-production-rvv-bin-index-precompute` | public full-cloud input | same | 当前 production RVV helper：centroid / normal centroid / pair math / viewpoint prep / bin index precompute | `run_test_compare` Std/RVV 各 9 个测试；`size_component` / `normalize_bins=false` fallback gate；over-range normal 语义 | `production_vfh_compute_default` | Phase 060 post-review mean Std `8.81437 ms`，RVV `5.37768 ms`，speedup `1.63906x`，5-run `1.62757x-1.65383x`，checksum `448016` 一致 | `accumulateVFHSPFHRVV` / `accumulateVFHViewpointRVV` 区域见 `vfmin.vf`、`vfmax.vf`、`vfcvt.rtz.x.f.v` | Phase 060 post-review Doctor `0E/0W/11S` | adopted production behavior | 当前同边界无进一步高优先级动作 |
| `vfh-histogram-scatter-rvv` | public full-cloud input | same, would touch histogram increment | 直接并行直方图更新或私有 bin 后合并 | 需要新增数值顺序和同 bin 冲突测试 | 需要专项消融和 board A/B | not_run | not_run | not_run | rejected/not_now | 同 bin 冲突和浮点顺序风险高；只有 profile 指向 scatter 为主成本时另开 topic |
| `vfh-point-type-or-parameter-expansion` | additional point types / parameters | 泛型 PointInT/PointNT、`normalize_bins=false`、`size_component`、`normalize_distances`、非 dense、subset indices、CVFH / OUR-CVFH | 超出当前 Phase 060 gate | 需要逐 gate fallback 和 production direct tests | 需要每个扩展边界重新 bench | not_run | not_run | not_run | deferred / separate follow-up | 扩大范围需新 phase，不作为当前 adopted 结论 |
