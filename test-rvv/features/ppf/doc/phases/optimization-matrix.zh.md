# PPF Optimization Matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| same-chain scalar reference | ordered index_i × full input | `PointXYZ` + `Normal` / float / AoS | test-only reference vs production `PPFEstimation::computeFeature` | `run_test_compare` passed Std/RVV | not_applicable | not_applicable | not_applicable | not_applicable | adopted for diagnostics | none |
| pair-feature batch RVV | ordered index_i × full input | `PointXYZ` + `Normal` / float / AoS | component diagnostic, test helper boundary | `run_test_compare` passed Std/RVV with `2e-3` tolerance | `bench_ppf` with `candidate_ppf_pair_feature_batch_rvv` | 5-run Milkv-Jupiter: `0.80, 0.79, 0.79, 0.79, 0.81` | `bench_ppf_rvv.asm` contains `computePPFPairFeatureBatchRVV` and RVV ops | `Errors=1, Warnings=1, Suggestions=8`; Error is 5/5 degradation | attempted / negative | Do not enter production; use result to prioritize `alpha_m` formula audit. |
| alpha_m closed-form scalar audit | ordered index_i × full input | `PointXYZ` + `Normal` / float / AoS | component diagnostic, formula equivalence | `run_test_compare` passed Std/RVV with `1e-5` tolerance | not_applicable | not_applicable | not_applicable | not_applicable | adopted for next diagnostic | Phase 030 can write RVV batch candidate against this helper. |
| alpha_m batch RVV | ordered index_i × full input | `PointXYZ` + `Normal` / float / AoS | component diagnostic, test helper boundary | `run_test_compare` passed Std/RVV with `2e-3` tolerance | `bench_ppf` with `candidate_ppf_alpha_m_batch_rvv`; QEMU smoke is log-shape only | 5-run Milkv-Jupiter: `1.57, 1.57, 1.56, 1.57, 1.57`; mean Std `415.5008 ms`, mean RVV `264.6650 ms` | `computePPFAlphaMBatchRVV` symbol range contains `vle32`, `vfdiv`, `vfsqrt`, `vmerge`, `vfnmsac`, `vfmacc`, `vse32` | repeated report `Errors=1, Warnings=1, Suggestions=10`; no alpha degradation Error, suggestions are metadata / binary identity | diagnostic positive / superseded by production probe | Phase 040 production-public evidence is now the decision source for production. |
| output staging / direct store audit | ordered index_i × full input | `PointXYZ` + `Normal` / float / AoS | component diagnostic | covered indirectly by Phase 010 staged candidate | no direct-AoS A/B yet | staged candidate board negative | staged candidate asm present | covered by Phase 010 doctor | attempted / negative for SoA staging | Direct-AoS gather is not the default next action; revisit only if `alpha_m` audit shows pair-feature math remains worth isolating. |
| production alpha_m batch RVV | ordered index_i × full input | exact `PointXYZ + Normal + PPFSignature` / float / AoS | production public overload `PPFEstimation::compute` / `computeFeature` | TDD RED: `run_test_rvv` failed on trace hit `0`; GREEN: `run_test_compare` Std 5 tests and RVV 6 tests passed | `public_ppf_compute`; QEMU smoke with `--case-filter public_ppf_compute` is log-shape only | 5-run Milkv-Jupiter production-public: `1.35, 1.35, 1.37, 1.39, 1.38`; mean Std `390.0784 ms`, mean RVV `284.9594 ms` | production helper `computePPFFeatureAlphaMRVV<PointXYZ, Normal, PPFSignature>` contains `vsetvli`, `vle32`, `vfdiv`, `vfsqrt`, `vmerge`, `vfnmsac`, `vfmacc`, `vse32` | repeated report `Errors=0, Warnings=0, Suggestions=2`; suggestions are environment metadata and binary identity | adopted production behavior in Phase 050; superseded by Phase 060 wider gate | Phase 060 closed point-type expansion; exact row remains representative baseline. |
| ppf-point-type-expansion | ordered index_i × full input | traits-gated source `RVVXYZAoSFloatLayout<PointInT>` + PPF normal AoS + exact `PPFSignature` / float / AoS | production public overload `PPFEstimation::compute` / `computeFeature` | RED: new RVV trace tests hit `0` before gate expansion; GREEN: `run_test_compare` Std 5 tests and RVV 9 tests passed; board RVV correctness 9/9 passed | `public_ppf_compute_pointxyzi_normal` and `public_ppf_compute_pointxyz_pointnormal`; QEMU smoke is log-shape only | `PointXYZI + Normal`: speedup `1.40, 1.43, 1.33, 1.41, 1.35`, mean Std `429.5662 ms`, mean RVV `310.3166 ms`; `PointXYZ + PointNormal`: speedup `1.33, 1.33, 1.31, 1.33, 1.35`, mean Std `412.4258 ms`, mean RVV `310.2756 ms` | production helper instances for `PointXYZ/Normal`, `PointXYZI/Normal`, and `PointXYZ/PointNormal` each contain RVV instructions | both repeated reports `Errors=0, Warnings=0, Suggestions=2`; suggestions are environment metadata and binary identity | adopted production behavior in Phase 060 | none high-priority inside current PPF topic; custom traits-compatible point types are gate-allowed but not individually board-covered. |
| doc-suite-parity-closeout | not_applicable | not_applicable | topic-local README / role docs / phase docs | link and artifact tracking checks | not_applicable | reuse Phase 060 production-public board evidence; no new timing | not_applicable | reuse Phase 060 Doctor summaries; no new Doctor needed | adopted in Phase 070 | none inside current PPF topic; evidence hardening and direct-AoS pair-feature revisit require separate phase authorization. |
