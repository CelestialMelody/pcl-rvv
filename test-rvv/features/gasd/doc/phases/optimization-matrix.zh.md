# Optimization Matrix

## 矩阵职责

这张矩阵记录 GASD topic 各个 candidate family 的证据状态、恢复条件和下一步，不记录对话过程。

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| fixed-grid histogram copy | shape / color shared tail | `PointXYZ` / `PointXYZRGBA` / `float` / contiguous histogram buffer | `copyShapeHistogramsToOutput`、`copyColorHistogramsToOutput` | `make run_test_compare` Std/RVV 4/4 通过 | `candidate_shape_copy_rvv` copy-only bench | 5-run median 1.050x，range 1.040x-1.080x | `build/asm/riscv/bench_gasd_rvv.asm` 命中 RVV copy 指令 | Errors=0，Warnings=0，Suggestions=2 | attempted / diagnostic-weak-positive | none for copy boundary；Phase 010 已继续 |
| shape sample projection | shape sample loop | `PointXYZ` / `float` / transformed samples | `computeFeature` 内样本投影 staging | `make run_test_compare` Std/RVV 6/6 通过 | `candidate_shape_projection_rvv` projection-staging bench | `phase010_shape_projection_fast` 5-run median 1.690x，range 1.690x-1.710x，checksum 一致 | `vlse32.v`、`vfmacc.vv`、`vfsqrt.v`、`vfdiv.vf`、`vfcvt.rtz.xu.f.v`、`vse32.v` | Errors=0，Warnings=0，Suggestions=2 | attempted / diagnostic-positive | none for projection staging；继续 Phase 020 |
| color hue projection | color sample loop | `PointXYZRGBA` / `float` / RGB hue | `GASDColorEstimation::computeFeature` 内 hue / hbin staging | `make run_test_compare` Std/RVV 7/7 通过 | `candidate_color_hue_rvv` hue/hbin-staging bench | `phase020_color_hue` 5-run median 1.920x，range 1.910x-1.980x，checksum 一致 | `vlse8.v`、`vzext.vf2`、`vmaxu.vv`、`vminu.vv`、`vfcvt.f.xu.v`、`vfrdiv.vf`、`vmerge.vvm`、`vse32.v` | Errors=0，Warnings=0，Suggestions=2 | attempted / diagnostic-positive | none for hue staging；继续 Phase 030 |
| trilinear interpolation arithmetic / index staging | per-sample interpolation | `PointXYZ` / `float` / Phase 010 projection staging | `addSampleToHistograms` 的 `INTERP_TRILINEAR` 算术 / 索引子段，不写 histogram | `make run_test_compare` Std/RVV 8/8 通过 | `candidate_trilinear_interpolation_rvv` trilinear-staging bench | `phase030_trilinear_interpolation` 5-run median 1.950x，range 1.560x-2.030x，checksum 一致 | `vfcvt.rtz.x.f.v`、`vfcvt.f.x.v`、`vle32.v`、`vse32.v`、`vadd`、`vmul`、`vfsub`、`vfmul` | Errors=0，Warnings=1，Suggestions=2 | attempted / diagnostic-positive | 继续 Phase 040 histogram write probe |
| histogram write probe | per-sample interpolation write | `PointXYZ` / `float` / flat histogram grid | `addSampleToHistograms` 的 trilinear write boundary（写回边界），RVV staging + scalar flat write | `make run_test_compare` Std/RVV 9/9 通过 | `candidate_trilinear_histogram_write_rvv` flat write bench | `phase040_histogram_write_probe` 5-run median 1.060x，range 1.050x-1.060x，checksum 一致 | RVV staging helper 命中；写回仍是 scalar boundary | Errors=0，Warnings=0，Suggestions=2 | attempted / diagnostic-weak-positive | 继续 Phase 050 Eigen-backed write probe |
| Eigen-backed histogram write probe | per-sample interpolation write | `PointXYZ` / `float` / `std::vector<Eigen::VectorXf>` histogram grid | 与 production 容器布局一致的 trilinear write boundary | `make run_test_compare` Std/RVV 10/10 通过 | `candidate_trilinear_eigen_histogram_write_rvv` Eigen-backed write bench | `phase050_eigen_histogram_write_probe` 5-run median 0.820x，range 0.800x-0.820x，checksum 一致 | RVV staging helper 命中；Eigen write 仍是 scalar boundary | Errors=1，Warnings=0，Suggestions=2；Error 是 5/5 退化频率，用于负向降级 | attempted / diagnostic-negative | 继续 Phase 060 production-shaped shape combined diagnostic |
| production-shaped shape combined diagnostic | shape sample loop + interpolation write + copy | `PointXYZ` / `float` / `std::vector<Eigen::VectorXf>` + descriptor buffer | test-only helper 组合 projection、trilinear Eigen write 和 shape copy | `make run_test_compare` Std/RVV 12/12 通过；板卡 `run_board_test` 12/12 通过 | `candidate_shape_combined_rvv` combined bench；QEMU 只作 log-shape smoke | `phase060_shape_combined` 5-run median 0.590x，range 0.590x-0.600x，checksum 一致 | `build/asm/riscv/bench_gasd_rvv.asm` 由当前 RVV bench 生成；RVV staging / copy 命中，Eigen write 仍是 scalar boundary | Errors=1，Warnings=0，Suggestions=2；Error 是 5/5 退化频率，用于负向降级 | attempted / production-shaped-diagnostic-negative | 继续 Phase 070 no-production closeout / profile recovery audit |
| no-production closeout / profile recovery audit | shape diagnostic decision | topic-local docs / no production patch | evaluation、roadmap、matrix 和 README 的 no-production 判断审计 | QEMU + board correctness 已在 Phase 060 前闭合 | not_applicable | not_applicable | not_applicable | not_applicable | adopted / no-production for current staged shape family | stop for user review；production probe 需用户授权 |
| public shape compute profile | public shape entry | `PointXYZ` / `float` / production descriptor | 未改 production 的 `GASDEstimation::computeFeature` public bench | QEMU `run_qemu_smoke` 12/12 + 12/12 通过 | `public_gasd_shape_compute` repeated board | `phase080_public_shape_compute` 5-run median 1.220x，range 1.210x-1.220x，checksum 一致 | not_applicable；本阶段不归因 RVV candidate asm | Errors=0，Warnings=0，Suggestions=2 | adopted / profile-only build-level signal | none；若继续深挖，先做 production segment profile |
| public color compute profile | public color entry | `PointXYZRGBA` / `float` / production descriptor | 未改 production 的 `GASDColorEstimation::computeFeature` public bench | QEMU `run_qemu_smoke` 12/12 + 12/12 通过 | `public_gasd_color_compute` repeated board | `phase080_public_color_compute` 5-run median 1.110x，range 1.100x-1.120x，checksum 一致 | not_applicable；本阶段不归因 RVV candidate asm | Errors=0，Warnings=0，Suggestions=2 | adopted / profile-only build-level signal | none；quadrilinear 仍需用户或 workload profile 指定 |
| quadrilinear color interpolation | per-sample color interpolation | `PointXYZRGBA` / `float` / color hue + histogram grid | `INTERP_QUADRILINEAR` hue 维度权重和写回 | planned | planned | pending | pending | pending | deferred / separate follow-up | 需要用户或 profile 指定 color quadrilinear 场景后再开 phase |
