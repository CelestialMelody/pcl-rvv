# moment_invariants optimization matrix

本矩阵记录 candidate family（候选实现族）、row source（行来源）、点类型 / layout、证据和恢复动作。`adopted` 只表示对应行的 evidence boundary（证据边界）已关闭，不外推到其它点型、其它入口或其它输出类型。

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| helper indexed moment accumulation RVV | indexed cloud | `PointXYZ / float / AoS` | test helper | QEMU Std/RVV helper pass via `make run_test_compare` | `mi_accumulation_indexed` | Phase 000 weak-positive: median `1.141x`, min `1.095x`, 0/5 below 1.0 | bench asm contains `vluxei32` / `vfredusum` | 0E/0W/2S | attempted: diagnostic positive | closed; superseded by production-public evidence |
| helper full-cloud moment accumulation RVV | full cloud | `PointXYZ / float / AoS` | test helper | QEMU Std/RVV helper pass via `make run_test_compare` | `mi_accumulation_full_cloud` | not primary board evidence | bench asm contains `vlse32` / `vfredusum` | not run as repeated board summary | deferred with evidence | reopen only if real full-cloud caller/profile appears |
| public-search-shaped helper replacement | KdTree k-neighbor query | `PointXYZ / float / AoS` | production-shaped diagnostic | Phase 000 correctness inherited; no production fallback dispatch | `mi_public_search_shape` | Phase 010 near-threshold weak-positive: median `1.031x`, min `1.026x`, 0/5 below 1.0 | test-only helper inlined; no production symbol | 0E/0W/3S, includes near-threshold suggestion | attempted: justified bounded production probe after user authorization | closed by Phase 030 production-public run |
| production indexed moment accumulation RVV | KdTree k-neighbor query | `PointXYZ / float / dense AoS / PointOutT=MomentInvariants` | public `computeFeature` -> indexed `computePointMomentInvariants` | `make run_test_compare`; `make run_board_test`; non dense fallback gtest | `mi_production_compute_feature` | Phase 030 weak-positive: median `1.067x`, min `1.023x`, 0/5 below 1.0 | `make check_production_rvv_asm` finds `vlux*ei32` / `vfred*sum` in production symbols | 0E/0W/2S | adopted | closed |
| PointXYZ-like production expansion | KdTree k-neighbor query | `PointXYZI / PointXYZRGB / PointXYZRGBA`, `float`, dense AoS, `PointOutT=MomentInvariants` | public `computeFeature` -> indexed `computePointMomentInvariants` | `make run_test_compare` Std/RVV 8/8 pass | typed `mi_production_compute_feature_*` cases | Phase 040 weak-positive: `PointXYZI 1.071x`、`PointXYZRGB 1.078x`、`PointXYZRGBA 1.078x` medians, all 0/5 below 1.0 | `make check_production_pointxyzi_rvv_asm`、`make check_production_pointxyzrgb_rvv_asm`、`make check_production_pointxyzrgba_rvv_asm` all pass | each 0E/0W/2S | adopted | closed |
| non-RVV / unsupported fallback | same public entry | non-RVV build、`PointOutT != MomentInvariants`、small neighbor count、non dense cloud、unsupported layout、u32 offset overflow | public helper fallback | Std build tests; non dense public case; compile-time no RVV path | not_applicable | not_applicable | not_applicable | not_applicable | adopted fallback boundary | closed for current scope |
| normal compound / custom point-type expansion | KdTree k-neighbor query | `PointXYZRGBNormal`、`PointXYZINormal`、custom traits-compatible xyz | production candidate only | missing dedicated correctness/fallback | missing dedicated bench | missing | missing | missing | deferred with evidence | new phase only if explicitly wanted |
| full-cloud production overload | full cloud | xyz AoS / `float` | `computePointMomentInvariants(cloud, j1, j2, j3)` | helper-only correctness exists, production caller missing | missing public bench | missing | missing | missing | not_now | reopen only with real caller/profile |
