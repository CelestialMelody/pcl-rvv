# ROPS optimization matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| central moments RVV | matrix bins | `Eigen::MatrixXf` / float / default and non-default bins | test-only `computeCentralMomentsRVV()` vs production private helper oracle | QEMU Std/RVV 3/3 | not_run；default 5x5 小矩阵且 RVV candidate 需要 staging，先不声明性能候选 | not_applicable for correctness-only Phase 000 | `dump_test_rvv` shows `computeCentralMomentsRVV` with `vsetvli` / `vle32.v` / `vfmul.vv` / `vfredusum.vs` | not_applicable：未生成 bench summary 或 EvidenceDecision | attempted-correctness-only | 不进入 production；继续 Phase 010 distribution matrix binning |
| distribution matrix binning | rotated local cloud | `pcl::PointXYZ` / float / AoS -> 2D bins | test-only `getDistributionMatrixRVV()` vs production private helper oracle | QEMU Std/RVV 4/4；覆盖 XY / XZ / YZ 和 max-bin clamp | `bench_rops_estimation` case `rops_distribution_matrix_binning,points=65536,bins=5` | `phase010_distribution_matrix_repeated` 5-run median 1.440x，min 1.420x，max 1.460x，0/5 退化，checksum match | test / bench asm shows `getDistributionMatrixRVV` with `vlse32.v` / `vfsub.vf` / `vfmul.vf` / `vfcvt.rtz.xu.f.v` / `vminu.vv` / `vmerge.vvm` / `vse32.v` | `log/board/phase010_distribution_matrix_repeated/evidence_doctor.md`：0 Error / 0 Warning / 2 Suggestion（环境 metadata、binary hash） | attempted-positive-diagnostic | 不单独进入 production；继续 Phase 020 rotate/projection + AABB，并保留 combined probe 条件 |
| projection / rotate + AABB | local transformed cloud | `pcl::PointXYZ` / float / AoS | test-only `rotateCloudRVV()` vs production private helper oracle | QEMU Std/RVV 6/6；覆盖 X/Y/Z 与 tilted axis，含 bench-shaped 1024 点样本 | `bench_rops_estimation` case `rops_rotate_cloud_aabb,points=65536,repeat=8` | `phase020_rotate_cloud_repeated` 5-run median 1.220x，min 1.200x，max 1.220x，0/5 退化，checksum match | test / bench asm shows `rotateCloudRVV` with `vlse32.v` / `vfmacc.vf` / `vsse32.v` / `vfmin.vv` / `vfmax.vv` / `vfredmin.vs` / `vfredmax.vs` | `log/board/phase020_rotate_cloud_repeated/evidence_doctor.md`：0 Error / 0 Warning / 2 Suggestion（环境 metadata、binary hash） | attempted-positive-diagnostic | 不单独进入 production；继续 Phase 030 combined rotate + distribution probe |
| combined rotate + distribution matrix | local transformed cloud -> rotated local cloud -> bins | `pcl::PointXYZ` / float / AoS + `Eigen::MatrixXf` bins | test-only `rotateCloudAndDistributionMatricesRVV()` / bench wrapper | QEMU Std/RVV 7/7；覆盖 combined rotate output、AABB 和三组 projection matrices | `bench_rops_estimation` case `rops_rotate_distribution_pipeline,points=65536,repeat=8` | `phase030_combined_rotate_distribution_repeated` 5-run median 1.630x，min 1.630x，max 1.640x，0/5 退化，checksum match | bench asm shows combined boundary with rotate `vlse32.v` / `vfmacc.vf` / `vsse32.v` / `vfredmin.vs` / `vfredmax.vs` and distribution `vfcvt.rtz.xu.f.v` | `log/board/phase030_combined_rotate_distribution_repeated/evidence_doctor.md`：0 Error / 0 Warning / 2 Suggestion（环境 metadata、binary hash） | attempted-positive-production-shaped-diagnostic | 已进入 Phase 040 production integration loop；PI5 采纳 / 回滚需用户确认 |
| production private helper dispatch | local transformed cloud -> production rotated cloud -> bins | `pcl::PointXYZ` / float / AoS | 真实 `ROPSEstimation::rotateCloud()` / `getDistributionMatrix()` production dispatch | QEMU Std/RVV compare 覆盖 production helper trace、小规模 fallback、private-helper non-dense fallback 和 public `compute()` non-dense fallback | `bench_rops_estimation` case `rops_production_rotate_distribution_pipeline,points=65536,repeat=8` | `phase040_production_direct_repeated` 5-run median 1.700x，min 1.690x，max 1.700x，0/5 退化，checksum match | bench asm shows production helper dispatch calls `pcl::detail::rops::rotateCloudRVV<PointXYZ>` and RVV ops | `log/board/phase040_production_direct_repeated/evidence_doctor.md`：0 Error / 0 Warning / 2 Suggestion（环境 metadata、binary hash） | adopted-production-detail | closed；长期文档为 `doc-rvv/features/rops_estimation-RVV.zh.md` |
| PointXYZ-like production detail expansion | local transformed cloud -> production rotated cloud -> bins | `RVVXYZAoSFloatLayout<PointInT>` / float AoS；代表 `PointXYZI`、`PointNormal`；非 f32 xyz layout fallback | 真实 typed `ROPSEstimation<PointT, Histogram<135>>::rotateCloud/getDistributionMatrix` dispatch | QEMU Std/RVV compare 覆盖 typed production trace、scalar-reference 对拍、helper invalid-input gate 和非 f32 layout fallback | `rops_production_pointxyzi_rotate_distribution_pipeline`、`rops_production_pointnormal_rotate_distribution_pipeline` | PointXYZI 5-run median 1.520x；PointNormal 5-run median 1.590x；均 0/5 退化、checksum match | bench asm has typed RVV helper instances with rotate/distribution RVV ops | 两组 `phase050_*_production_detail_repeated/evidence_doctor.md`：均 0 Error / 0 Warning / 2 Suggestion | adopted-representative-typed-production-detail | closed；完整 public workload/profile、自定义点型逐个性能或 metadata hardening 需另开目标 |
