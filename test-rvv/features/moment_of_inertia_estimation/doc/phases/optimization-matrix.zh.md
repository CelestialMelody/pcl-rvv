# Moment of inertia estimation optimization matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| fused xyz reductions | ordered indexed cloud | `pcl::PointXYZ`, `float`, AoS（结构数组）stride load / indexed gather | `computeMeanValue`、indexed covariance、single-axis `calculateMomentOfInertia`、OBB projection extrema 的 test-only helper | `run_test_compare`；同一输入下 Std/RVV 对拍均值、AABB、协方差、惯性矩、OBB extrema | board `run_bench_compare`，case-filter `moi_reductions` | 5-run repeated：median 2.082x，min 2.048x，max 2.133x，bucket `positive` | `dump_bench_rvv` 出现 `vluxei32`、`vfredusum/vfredmin/vfredmax`、`vfmacc` | Errors=0，Warnings=0，Suggestions=1（binary identity missing） | attempted_positive_diagnostic | 可进入 PI1；不能直接写成 production direct |
| projected cloud covariance fusion | ordered indexed cloud, per-axis projection | `pcl::PointXYZ`, `float`, AoS | `getProjectedCloud` + projected covariance 合并，避免每个角度 materialize projected cloud（实体化投影点云） | `run_test_compare`；materialized projection reference vs fused formula（融合公式）RVV helper | board `run_bench_compare`，case-filter `moi_projected_covariance` | 5-run repeated：median 1.247x，min 1.237x，max 1.353x，bucket `positive` | `measureProjectedCovariance` 范围内有 `vluxei32`、`vfmacc`、`vfnmsac`、`vfredusum` | Errors=0，Warnings=0，Suggestions=1（binary identity missing） | attempted_positive_diagnostic | PI1 冻结 projected covariance production plan；production patch 需用户授权 |
| production mean/AABB RVV helper | public `MomentOfInertiaEstimation<PointT>::compute()` | `RVVXYZAoSFloatLayout<PointT>` PointXYZ-like traits gate、`float`、indexed AoS、32-bit byte offset gate | historical `computeMeanValue()` private dispatch；当前源码已回滚 | historical `make run_test_compare`；GREEN 后 Std 3/3、RVV 4/4 | `moi_public_compute` production-public bench，angle step 45 | 5-run repeated：median 1.018x，min 0.987x，max 1.025x，B/A < 1 为 2/5，bucket `neutral` | historical public bench binary 中有 `vluxei32`、`vfred*` | Errors=1（`ba_degradation_frequency`），Warnings=0，Suggestions=2 | rolled_back_after_user_confirmation | 不再继续；phase040 已取代当前生产方向 |
| projected covariance production probe | public angle scan 内 `getProjectedCloud()` + projected `computeCovarianceMatrix()` 替换候选 | PointXYZ-like traits gate、`float`、indexed AoS、32-bit byte offset gate | 真实 production fusion helper，避免每个 axis materialized projected cloud | RED 缺 `computeProjectedCovarianceRVV`；GREEN 后 `make run_test_compare` Std 3/3、RVV 4/4 | `moi_public_compute` production-public bench，angle step 45 | 5-run repeated：median 1.984x，min 1.877x，max 2.076x，B/A < 1 为 0/5，bucket `positive` | public bench binary 中有 `vluxei32`、`vfmacc`、`vfnmsac`、`vfredusum` | Errors=0，Warnings=0，Suggestions=1（binary identity missing） | adopted_production_behavior | 下一阶段补 point type expansion；当前 `PointXYZ` 证据不能外推到全部 PointXYZ-like 类型 |
| PointXYZ-like production evidence expansion | phase040 adopted projected covariance helper + phase050 typed public board | `PointXYZI`、`PointXYZRGB`、`PointXYZRGBA`、`PointXYZRGBNormal`、`float`、AoS、32-bit byte offset gate | public `MomentOfInertiaEstimation<PointT>::compute()` typed scope | `make run_test_compare`；typed public compute correctness、helper hit tests、std build / empty input or indices / non-float xyz layout fallback isolation | board `moi_public_compute_pointxyzi` / `moi_public_compute_pointxyzrgb` / `moi_public_compute_pointxyzrgba` / `moi_public_compute_pointxyzrgbnormal`，5-run repeated board | 4 组 typed public board 全部 positive；median 2.132x / 2.395x / 2.487x / 2.784x，0/5 B/A < 1 | public bench binary 继续命中 `vluxei32`、`vfmacc`、`vfnmsac`、`vfredusum` | 四组 Evidence Doctor 都是 Errors=0，Warnings=0，Suggestions=1（binary identity missing） | adopted_common_typed_scope | 默认停止同类继续；custom point type / exotic layout 另开 phase |
