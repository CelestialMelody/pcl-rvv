Bilateral RVV asm evidence (bench_bilateral_rvv.full.asm)

Production dispatch:
- `pcl::BilateralFilter<pcl::PointXYZI>::applyFilter` calls `pcl::computePointWeightRVV<pcl::PointXYZI>`.
- `pcl::BilateralFilter<pcl::PointXYZINormal>::applyFilter` calls `pcl::computePointWeightRVV<pcl::PointXYZINormal>`.

Representative RVV instruction evidence from the dump:
- `vsetvli`
- `vle32.v`
- `vluxei32.v`
- `vfsub.vv`
- `vfmul.vv`
- `vfmacc.vv`
- `vse32.v`

Interpretation:
- `PointXYZI` and `PointXYZINormal` both instantiate the production RVV helper through the shared XYZ + writable float intensity gate.
- The explicit RVV gather/exp/store path remains present after replacing the exact `PointXYZI` gate with the traits-based gate.
- Tail BF/W accumulation remains scalar-order constrained by the local `no-tree-vectorize` pragma; this evidence file does not treat auto-vectorized reduction as the production RVV proof.
