# GICP Optimization Matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| residual dense-row RVV reduction | post-correspondence dense-row | test-only double arrays | `OptimizationFunctorWithIndices` residual math component | QEMU Std/RVV current aggregate 10 tests pass；board RVV smoke 10 tests pass | `run_bench_all_smoke` / board repeated | `1.347x` median，0/5 B/A < 1，`positive` | `bench_gicp_rvv.asm` 有 RVV 指令，但非 production attribution | Warning: reduction contract mismatch | PI1 candidate | public-entry profile |
| residual indexed-gather RVV reduction | post-correspondence indexed source/target gather；接近 `tmp_idx_src_` / `tmp_idx_tgt_` | test-only double arrays + uint64 indices | `OptimizationFunctorWithIndices` residual gather + math component | QEMU Std/RVV current aggregate 10 tests pass；board RVV smoke 10 tests pass | `run_bench_all_smoke` / board repeated | `1.255x` median，0/5 B/A < 1，`positive` | asm 有 `vluxei64.v`，但非 production attribution | Warning: reduction mismatch + long tail | PI1 candidate strengthened | public-entry profile，重点看 optimizer 占比 |
| dfddf loop dense-row RVV reduction | post-correspondence dense-row | test-only double arrays | `OptimizationFunctorWithIndices::dfddf()` per-correspondence accumulator loop | QEMU Std/RVV Hessian diagnostic 2 tests pass | board repeated `dfddf_loop_dense_repeated` | `1.410x` median，0/5 B/A < 1，`positive` | bench binary attribution pending refresh | Warning: reduction mismatch | next PI candidate | production indexed-gather probe for `dfddf()` |
| covariance post-KNN RVV reduction | post-nearestKSearch neighborhood rows | test-only double arrays, k=20 / k=7 | `computeCovariances()` k-loop before SVD | QEMU Std/RVV current aggregate 10 tests pass；board RVV smoke 10 tests pass | `run_bench_all_smoke` / board repeated | `1.151x` median，0/5 B/A < 1，`weak_positive` | `bench_gicp_rvv.asm` 有 RVV 指令，但非 production attribution | Warning: reduction mismatch + long tail | secondary PI1 candidate | profile KdTree/SVD dilution before production |
| production GICP cost-only dispatch | public GICP entry | traits-gated xyz AoS, `Scalar=float` | `OptimizationFunctorWithIndices::operator()` line-search cost | QEMU Std/RVV public smoke pass | board repeated 1024 点 + 4096 点扩展 | 1024 median `1.019x` neutral；4096 median `1.011x` neutral | production asm attribution pending | Warning: reduction mismatch；near-threshold | do not adopt current patch | ask user whether to rollback cost-only patch or pivot to `dfddf()` production probe |
| production GICP `dfddf()` clean dispatch | public GICP entry | traits-gated xyz AoS, `Scalar=float`, `PointXYZ -> PointXYZ` | `OptimizationFunctorWithIndices::dfddf()` accumulator loop；`operatorCostRVV()` 已移除 | QEMU Std/RVV 10 tests pass；board RVV smoke 10 tests pass；public transform reference locked | board repeated 1024 点 + 4096 点扩展 | 1024 median `1.080x` weak_positive；4096 median `1.058x` weak_positive | historical full asm 中 `dfddf()` calls `dfddfLoopRVV()` 且 helper 有 RVV 指令 | 1024 Errors=0 Warnings=1；4096 Errors=0 Warnings=2 | rolled back / no-production due to low benefit | topic closeout |
| `dfddfLoopRVV()` matrix gather width | public GICP entry | traits-gated xyz AoS, `Scalar=float`, `PointXYZ -> PointXYZ` | Phase 003 helper 内部把 `mahalanobis_` matrix gather 从 `vluxei64` 临时改为 `vluxei32` | QEMU Std/RVV 10 tests pass | board repeated 1024 点 | 1024 median `1.073x` weak_positive，未优于 Phase 003 `1.080x` | asm attribution 未作为采纳证据刷新 | Errors=0 Warnings=1 | attempted / do not adopt | 已回退本阶段小改动；当前源码保持 Phase 003 形态 |
| rollback no-production closeout | public GICP entry | all production template instances | 删除 GICP 生产 RVV dispatch，保留 topic-local 诊断证据 | QEMU Std/RVV 10 tests pass | no new board bench needed | historical production probes retained as decision evidence | production source has no RVV helper | registry fresh | no-production | commit GICP topic only |
