# Optimization Roadmap

本路线图记录 `transformation_estimation_2D` 的跨阶段候选搜索空间。阶段 `plan/result` 负责本阶段闭环，`doc/phases/optimization-matrix.zh.md` 记录证据状态；本文件保存后续仍可恢复的 candidate family（候选族）、暂缓 / 拒绝路线和默认恢复动作。

## 当前边界

当前 production 源码保留四条已采纳 / 保留的 RVV behavior（RVV 行为）：ordered-cloud-pair
traits-gated generic public dispatch、source-indexed exact `PointXYZ -> PointXYZ`
public dispatch、Phase 112 已采纳的 source-indexed exact `PointXYZI -> PointXYZI`
public dispatch，以及 Phase 107 新接入、当前经 Phase 109 复核后继续保留的 dual-indexed exact
`PointXYZ -> PointXYZ` public dispatch。Phase 107/112 接入后 correctness 为 Std/RVV `84/84`；ordered generic
Phase 107 public board 16/16 positive，source-indexed exact public board 为
`4.157x / 4.814x / 4.615x`，Phase 109 dual-indexed exact family A/B 为
`1.080x / 1.661x / 1.646x`。source-indexed generic widening 已根据 Phase 106
20-run representative variance negative 收窄回 Phase 091 exact gate；Phase 103/104
仍只是 guarded probe / not adopted。correspondence exact trial 在 Phase 107 family A/B
中 256K 出现 `4/20` below-1，decision bucket 为 negative，production dispatch 已退回。
Phase 100 / 101 的 generic row-source diagnostics 仍为 negative，不扩大到泛型点型。
未逐类型上板的自定义点型、RGB/RGBA 和 `Scalar=double` 仍不自动继承这些结论。
用户已确认有收益优化可以接入并倾向先提交。Phase 108 已完成提交前 phase 文档压缩：
`doc/phases/README.zh.md` 改为短入口，历史阶段转入 `doc/phases/history.zh.md` 压缩索引，
不改变 Phase 107 production patch 的生产边界。Phase 108 完成后已按 topic-only
策略提交，不提交 raw logs。Phase 109 已完成用于复核 dual-indexed exact 4K caveat 的独立
20-run variance：4K/64K/256K median B/A 为 `1.080x / 1.661x / 1.646x`，4K 有
`1/20` below-1，Board Doctor `0/3/0`。该证据支持保留 exact dual-indexed dispatch，
但 4K caveat 继续保留。

Phase 110 在 source-indexed public overload 中新增 exact `PointXYZI -> PointXYZI` 有界
production probe（生产探针）。接入后 correctness 仍为 Std/RVV `84/84`；QEMU source-indexed
PointXYZI public smoke Doctor `0/0/0`；asm 可归属到 `PointXYZI -> PointXYZI`
source-indexed public overload；独立 board label
`source_indexed_pointxyzi_public_phase110_repeated` 的 20-run median B/A 为
`3.979x / 3.547x / 3.602x`，三个规模 `B/A<1=0/20`，Board Doctor `0/3/0`。该证据
支持 exact `PointXYZI -> PointXYZI` production dispatch；Phase 112 已按用户确认写成
adopted / retained，但不能外推成 source-indexed generic widening。

Phase 111 已完成 Phase 106 Normal 类负向样本审计，不修改 production gate。审计确认
`PointNormal` 和 `PointXYZINormal` 都是 48B stride，Phase 106 的 below-1 / long-tail 事件
集中在这些 Normal 类组合；`PointNormal->PointNormal 256K` 仍以 `7/20` below-1 和 Board
Doctor Error 为主证据。因此 Normal 类 source-indexed generic widening 不建议接入，后续若恢复
必须另开 exact Normal bounded probe 或 profiling phase，并使用独立 board label / evidence dir。
本轮用户已确认不再推进 Normal 类 source-indexed 优化探索；2D-only load2、exact Normal
specialization、selected `x/y` materialization 和 chunk/profile ablation 仅作为未推进候选记录，
不再列为默认恢复队列。

Phase 113 是 production doc closeout（生产长期文档收尾）和 generated log hygiene（生成日志卫生）
阶段，不改变 RVV 算法或 production gate。该阶段修复 Phase 112 后暴露的 closeout process gap：
长期 `doc-rvv` 不能只做 freshness sync，必须按 Production Doc Closeout Gate 补齐当前采用的优化方式、
正确性与高效性证据链、Traceability Map、fallback 矩阵、未采纳方向和 log policy。Phase 113
同时把早先被 Git 跟踪的 TE2D `log/**` generated files 从索引中移除，保留本地文件，由
`test-rvv/.gitignore` 接管；后续若需要提交 evidence logs，必须另开 evidence-log phase 做脱敏和提交边界审计。

当前 topic 边界：

- 目标入口为 `TransformationEstimation2D` 四个 `estimateRigidTransformation` overload。
- Phase 010/020 已覆盖顺序点云对（ordered-cloud-pair，source/target 按相同下标一一对应）的 `PointXYZ -> PointXYZ`、`Scalar=float` 诊断。
- Phase 050 最新证明 exact `PointXYZ -> PointXYZ`、`Scalar=float`、dense finite ordered-cloud-pair 的 production-public probe 在目标板卡上 4K/64K/256K 分别达到 `4.222x / 5.310x / 4.947x`，Doctor `0/0/0`，支持保留窄范围 production patch。
- Phase 030 已为 source-indexed-cloud-pair、dual-indexed-cloud-pair 和 correspondence-pair 建立 materialize-to-ordered test-only candidate；Phase 090 按同输入语义刷新后 materialize Doctor 为 `1/1/6`，不能继承 ordered-cloud-pair 的 production 结论。
- Phase 070 已为 `PointXYZ`、`PointXYZI`、`PointNormal`、`PointXYZINormal` 及四组 mixed pair 补 traits gate、fallback、QEMU、asm、板卡和 Doctor；board Doctor 为 `0/3/2`，异常已按 case 分桶，属于历史 test-only diagnostic。
- Phase 080 已为同一代表性范围完成 production public direct correctness、fallback、QEMU、production-symbol asm、board repeated 和 Doctor；15 个 case 每 case `B/A<1=0/5`，但 `PointNormal -> PointNormal 64K` 为 negative；用户已确认采纳当前 patch。
- Phase 090 已完成三类 row source 的 direct gather test-only candidate，并按同输入语义刷新 materialize-to-ordered：source-indexed direct gather 为 `1.199x / 1.135x / 1.130x` 且每 case `B/A<1=0/5`，已支持 Phase 091；dual-indexed 和 correspondence 有 unstable / negative bucket，只能做 guarded production probe 或 staging/profile。
- Phase 091 已完成 source-indexed `PointXYZ -> PointXYZ` public production probe：QEMU Doctor `0/0/0`、production asm 46 RVV lines、Milkv-Jupiter public board `4.103x / 4.818x / 4.575x`、board Doctor `0/0/0`；用户已确认采纳该 narrow row-source patch。当前 aggregate correctness 已在 Phase 103 后更新为 Std/RVV 84/84；Phase 101 的 80/80 保留为历史快照。
- Phase 092 已补 source-indexed 同一 production boundary 内的 RVV-vs-RVV family A/B：direct public 相对 materialize+ordered public 的 B/A 为 4K `1.089x`、64K `1.044x`、256K `1.037x`，overall bucket `weak_positive`，Doctor `0/0/2`；这支持保留 current direct gather family，但不改 production dispatch。
- Phase 110 已补 source-indexed exact `PointXYZI -> PointXYZI` public production probe：QEMU Doctor `0/0/0`、production asm 命中、Milkv-Jupiter 20-run board `3.979x / 3.547x / 3.602x`、board Doctor `0/3/0`。Phase 112 已按用户确认采纳 exact gate。
- Phase 093/107/109 已补 dual-indexed 同一 production boundary 内的 RVV-vs-RVV family A/B：Phase 109 direct public 相对 materialize+ordered public 的 B/A 为 4K `1.080x`、64K `1.661x`、256K `1.646x`，overall bucket `positive`，Doctor `0/3/0`；4K `1/20` below-1 作为小规模 caveat 保留。当前 production patch 已接入 exact dual-indexed dispatch，并已随 topic-only commit 保留。
- Phase 094/107 已完成 correspondence guarded production probe / staging-profile 证据化：public correspondence probe 对 public scalar path 曾是 positive，但 Phase 107 同边界 family A/B 在 256K 出现 `4/20` below-1，overall bucket `negative`，因此 correspondence production dispatch 已退回。Phase 107 不改变 ordered / source-indexed / dual-indexed 的保留状态。
- Phase 095 已完成 correspondence staged-indices + dual-indexed public profile：staged-dual 与 direct correspondence public RVV 的 D/S median 为 4K `0.833x`、64K `0.729x`、256K `0.848x`，D/S<1 分别为 `20/20`、`20/20`、`15/20`，Board Evidence Doctor 为 `3/2/0`。该路线为 `attempted / profile-only / rejected for production switch`，不修改 correspondence dispatch。
- Phase 096 已完成 correspondence locality/order profile：identity、reverse、shuffled、strided 四类 query/match 分布下 direct/staged D/S 均不支持 staged-dual switch，overall `negative`，Board Evidence Doctor 为 `12/5/0`。该路线为 `attempted / profile-only / negative / no dispatch switch`，不自动回滚 Phase 094 guarded patch。
- Phase 097 已完成 correspondence component ablation：strided full-anchor D/S 为 4K `0.834x`、64K `0.761x`、256K `0.841x`，Board Evidence Doctor 为 `3/2/0`。component no-solve 只能提供 ingress / gather / materialize 成本线索，不能直接推出 production dispatch switch。
- Phase 098 已完成 correspondence chunked xyz staging candidate：20-run board D/C 为 4K `0.813x`、64K `0.941x`、256K `0.983x`，Board Evidence Doctor 为 `3/3/0`。chunked staging 只作为 profile-only negative evidence，不进入 production dispatch。
- Phase 099 已完成 source-indexed generic PointXYZ-like diagnostic：correctness Std/RVV 64/64；QEMU source-indexed generic Doctor `0/0/0`；asm `source_indexed_generic_candidate_lambda_boundary` 546 RVV lines；Milkv-Jupiter 5-run board Doctor `5/10/1`。`PointXYZ->PointXYZ` 三个规模均为 negative，`PointXYZI->PointXYZI` 和 `PointNormal->PointNormal` 的 256K 也为 negative；因此不修改 production dispatch。
- Phase 100 已完成 dual-indexed generic PointXYZ-like diagnostic：correctness Std/RVV 72/72；QEMU dual-indexed generic Doctor `0/0/0`；asm `dual_indexed_generic_candidate_lambda_boundary` 880 RVV lines；Milkv-Jupiter 5-run board Doctor `13/17/1`。64K/256K 和 mixed pair 多数为 negative；因此不修改 production dispatch。
- Phase 101 已完成 correspondence generic PointXYZ-like diagnostic：correctness Std/RVV 80/80；QEMU correspondence generic Doctor `0/0/0`；asm `correspondence_generic_candidate_lambda_boundary` 860 RVV lines；Milkv-Jupiter 5-run board Doctor `11/19/2`。4K same-type 多为 weak-positive，但多数 64K/256K same-type 与 mixed pair 为 negative；因此不修改 production dispatch。
- `Scalar=double`、未逐类型上板的自定义点型和 RGB/RGBA 仍需要独立证据；dual-indexed 只在 Phase 107 exact `PointXYZ -> PointXYZ` 范围内进入当前 production patch，correspondence 当前回退到标量 public overload。

## 候选搜索空间

| candidate family | idea source | applies to | expected benefit | risk / unknown | required evidence | status | next phase |
| --- | --- | --- | --- | --- | --- | --- | --- |
| scalar baseline characterization | 当前 production 源码 | all row source policies | 建立 public semantics、耗时基线和 correctness reference（正确性参考链路）。 | 非法 index / correspondence 不宜写成安全合同。 | public semantics tests、QEMU correctness、bench baseline。 | partial / current scalar truth | `row-source-board-repeated` for current candidate comparison |
| two-pass centered fused 2D correlation accumulator | Phase 010 / Phase 020 证据 | ordered-cloud-pair, dense finite `PointXYZ`, `Scalar=float` | 避免两份 demean dynamic matrix（动态矩阵）和 Eigen correlation multiply。 | 已在窄范围 production public path 获得正向板卡证据。 | same-chain tests、bench、asm attribution、board repeated summary、Evidence Doctor。 | retained diagnostic and production-backed narrow shape | review current patch |
| production-public fused 2D correlation probe | Phase 050 证据 | exact `PointXYZ -> PointXYZ`, `Scalar=float`, dense finite ordered-cloud-pair | 历史真实 public overload 窄范围分流，已被 Phase 080 generic gate 覆盖。 | 最新板卡 4K/64K/256K 为 4.222x/5.310x/4.947x，Doctor 0/0/0。 | PI3 correctness、QEMU smoke、asm、board repeated、Evidence Doctor。 | historical production evidence | keep as exact-gate baseline only |
| traits-gated generic xyz ordered-cloud-pair diagnostic | Phase 070 证据 | `PointXYZ`、`PointXYZI`、`PointNormal`、`PointXYZINormal`，`Scalar=float`，source/target 独立 AoS traits gate | test-only generic candidate；读取各自 x/y/z offset 和 stride，额外字段不参与 2D 计算。 | 历史 Std/RVV 24/24；16-case QEMU；generic boundary asm；Milkv-Jupiter 5-run representative bench。 | source/target traits gate、same/mixed correctness、fallback、QEMU、asm、board repeated、Doctor。 | historical evidence-closed / test-only；board Doctor 0/3/2 | Phase 080 |
| traits-gated generic xyz ordered-cloud-pair production dispatch | Phase 080 证据 | 同上四类点型和四组 mixed pair；`Scalar=float` | 真实 public overload；失败回退 iterator scalar path。 | 当前 aggregate Std/RVV 84/84；16-case public QEMU；production-symbol asm；Milkv-Jupiter 5-run public board。 | public direct correctness、fallback、QEMU、production asm、board repeated、Doctor、registry。 | adopted-by-user；board Doctor 0/6/0，含一个 negative bucket | keep adopted; do not extend to row source |
| direct gather row-source candidate | Phase 090 证据 | source-indexed、dual-indexed、correspondence first under `PointXYZ -> PointXYZ`, `Scalar=float` | 避免完整 materialize-to-ordered 临时点云，按 row source 直接 gather xyz。 | source-indexed 正向；dual-indexed 64K/256K 与 correspondence 仍有退化，不能 clean adopt。 | correctness、selected-row fallback、QEMU、asm、board repeated、Doctor 已完成；production 接入前另开 PI1。 | attempted / diagnostic closed | source-indexed 已进入 Phase 091；dual/correspondence guarded queue |
| source-indexed production dispatch | Phase 091 / 092 证据 | source-indexed `PointXYZ -> PointXYZ`、`Scalar=float`、valid indices、dense finite | 真实 public overload direct gather RVV candidate；失败回退 iterator scalar path。 | public Std/RVV positive 证明 public RVV 快于 public scalar；Phase 092 family A/B 只 weak-positive。 | 接入后 correctness/fallback、QEMU、production-symbol asm、board repeated、Doctor、registry 已完成；same-boundary RVV-vs-RVV A/B 已补。 | adopted-by-user / direct gather family kept with weak-positive caveat | next guarded row source |
| source-indexed PointXYZI exact production dispatch | Phase 110/112 证据 | source-indexed exact `PointXYZI -> PointXYZI`、`Scalar=float`、valid source indices、dense finite | 真实 public overload 的 adopted exact dispatch；失败回退 iterator scalar path；不扩大到 generic PointXYZ-like。 | 20-run public board 三个规模 positive，但 Doctor 有长尾 Warning；Phase 112 已按用户确认采纳 exact gate。 | correctness Std/RVV 84/84、QEMU smoke、production asm、20-run board repeated、Doctor、registry 已完成。 | adopted / retained exact gate | keep exact gate; future widening needs independent phase |
| source-indexed generic PointXYZ-like diagnostic | Phase 099 证据 | source-indexed row source 下四类代表性点型和四组 mixed pair；`Scalar=float` | test-rvv generic direct gather candidate；source indexed gather 与 target prefix strided load 分别走 traits gate。 | `PointXYZ->PointXYZ` 三个规模均为 negative，且 `PointXYZI` / `PointNormal` 256K negative；mixed 64K 多数正向但不能覆盖负向 same-type。 | correctness/fallback、QEMU smoke、asm、5-run board、Doctor、registry 已完成。 | attempted / mixed-negative / no production change | completed; do not widen Phase 091 |
| source-indexed generic public representative variance | 用户继续技术证据化；Phase 103/104 guarded probe | source-indexed public overload；`PointXYZ`、`PointXYZI`、`PointNormal`、`PointXYZINormal` same-type 和四组 mixed pair；`Scalar=float` | 使用真实 public source-indexed generic probe 的同一边界，以独立 board label / evidence dir 做 20-run variance 复核。 | Phase 106 出现 3 个 negative bucket；`PointNormal->PointNormal 256K` 有 `7/20` below-1 并触发 Doctor Error，不能 clean-adopt。 | current 84/84 correctness、QEMU smoke、production-symbol asm、QEMU Doctor、20-run board repeated、board Doctor 和 registry 已完成；board Doctor `1/27/0`。 | completed / negative for full generic public variance / guarded | user decision: keep guarded narrow/probe, investigate negative cases, or request rollback |
| source-indexed Normal load-shape mitigation | Phase 111 负向归因 | possible exact `PointNormal` / `PointXYZINormal`, `Scalar=float`, source-indexed row source | 可能通过 2D-only load2、exact Normal specialization、selected `x/y` materialization 或 chunk/profile ablation 降低 48B AoS stride + gather/stride 的访存压力。 | 目前只是原因假设和候选形态，没有同边界 correctness、asm、board repeated 或 Doctor；用户已决定不继续推进。 | 若未来重新打开，必须新建独立 phase、独立 board label / evidence dir，并从 correctness、QEMU/asm、20-run board 和 Evidence Doctor 重新闭环。 | not_planned / user-declined further Normal optimization | none; do not resume by default |
| production doc closeout and generated log hygiene | Phase 113 / 用户反馈 | `doc-rvv`、phase index、roadmap、matrix、Handoff、TE2D generated logs | 让 production closeout 不再降格成 freshness sync；让 tracked generated logs 不再因复跑污染提交边界。 | 不改变 RVV 算法；`log/**` 删除是从 Git 索引删除而非本地 evidence 删除。 | Production Doc Closeout Gate 审计、`git rm --cached` tracked logs、artifact tracking scan、`evidence_status`、YAML parse、`git diff --check`。 | completed / committed | Commit `4ba8e63fd`; do not reopen except for future evidence-log phase |
| test-support responsibility split | Phase 114 / 用户反馈 + Commit A rule update | `include/te2d.h`、`include/impl/te2d_*.hpp`、README、code map、phase README、roadmap、matrix、Handoff | 将多职责测试支撑按 responsibility-first（职责优先）拆分；行数阈值只作为二次拆分信号。 | 不改变 production source、算法语义、case 名、Make target、bench 输出合同或性能结论；旧内部入口删除且不保留别名。 | `run_test_compare`、`record_qemu_correctness_state`、`evidence_status`、旧入口引用扫描、YAML parse、`git diff --check`、staged set 审计。 | completed / ready for topic-only Commit B | create topic-only Commit B |
| dual-indexed generic PointXYZ-like diagnostic | Phase 100 证据 | dual-indexed row source 下四类代表性点型和四组 mixed pair；`Scalar=float` | test-rvv generic direct gather candidate；source / target 两侧 indexed gather 分别走 traits gate。 | `PointXYZ` 仅 4K weak-positive，64K/256K negative；`PointXYZI` 全规模 negative；`PointNormal` / `PointXYZINormal` 大规模 negative；mixed 64K 全部 negative。 | correctness/fallback、QEMU smoke、asm、5-run board、Doctor、registry 已完成。 | attempted / negative / no production change | completed; do not widen Phase 093 |
| correspondence generic PointXYZ-like diagnostic | Phase 101 证据 | correspondence row source 下四类代表性点型和四组 mixed pair；`Scalar=float` | test-rvv generic direct gather candidate；source/query 与 target/match 两侧 indexed gather 分别走 traits gate。 | 4K same-type 多为 weak-positive，但多数 64K/256K same-type 与 mixed pair 为 negative。 | correctness/fallback、QEMU smoke、asm、5-run board、Doctor、registry 已完成。 | attempted / negative / no production change | completed; do not restore Phase 107 rolled-back correspondence dispatch |
| dual-indexed production-detail family A/B | Phase 093 / 107 / 109 证据 | dual-indexed `PointXYZ -> PointXYZ`、`Scalar=float`、valid source/target indices、dense finite | 真实 dual-indexed public direct gather RVV 与 materialize+ordered public RVV 在同一 RVV binary 内比较。 | Phase 109 64K / 256K positive，4K median positive 但有 `1/20` below-1；当前只能支撑 exact narrow boundary。 | Phase 107 correctness/fallback、QEMU、production-symbol asm、board repeated、Doctor、registry 已完成；Phase 109 独立 20-run variance 已完成。 | retained / positive with 4K caveat | no gate change; next bounded candidate only |
| correspondence guarded production probe | Phase 094 / 107 evidence | correspondence `PointXYZ -> PointXYZ`、`Scalar=float`、valid query/match correspondences、dense finite | 真实 correspondence public overload曾试接 RVV；public probe positive，family A/B 已补。 | Phase 107 family A/B 在 256K 出现 `4/20` below-1，decision bucket negative。 | correctness/fallback、QEMU smoke、board public/family evidence、Evidence Doctor、registry 已完成。 | attempted / rolled back / not adopted | no production dispatch; new candidate only with explicit phase |
| correspondence staged-indices + dual public profile | Phase 095 evidence | correspondence `PointXYZ -> PointXYZ`、`Scalar=float`、valid query/match correspondences、dense finite | 将 query/match 拷成连续 indices 后调用 dual-indexed public RVV，用于分离 correspondence 结构体索引读取和后段 gather/solve 成本。 | staging copy 成本超过收益；三个规模 direct/staged 均为 negative，不能作为 dispatch switch。 | staged-dual correctness、QEMU smoke、asm、20-run board、Evidence Doctor、registry 已完成。 | attempted / profile-only / rejected for production switch | no dispatch switch; locality/order 和 component ablation 均已完成且未支持 staged-dual |
| correspondence locality/order profile | Phase 096 evidence | correspondence `PointXYZ -> PointXYZ`、`Scalar=float`、valid query/match correspondences、dense finite | 按 identity、reverse、shuffled、strided 分布比较 direct、staged-dual、materialize 三条 public RVV 路径，解释访问顺序与局部性。 | 四类 profile 均未支持 staged-dual switch；shuffled 中 materialize 更快但不是当前 production dispatch 证据。 | locality/order correctness、QEMU smoke、asm、20-run board、Evidence Doctor、registry 已完成。 | attempted / profile-only / negative / no dispatch switch | component ablation 已由 Phase 097 完成；下一步只能是 chunked xyz staging 或用户决策 |
| correspondence component ablation | Phase 097 evidence | correspondence `PointXYZ -> PointXYZ`、`Scalar=float`、valid query/match correspondences、dense finite、strided profile | 拆分 query/match 展开、correspondence 结构读取、点云 gather、math/reduction 和 materialize 成本，定位 256K 长尾来源。 | full-anchor D/S 仍为 negative；component no-solve 与 full estimate sink 不同，不能直接推出 production dispatch。 | QEMU log shape、asm attribution、10-run board representative repeated、Evidence Doctor 已完成。 | attempted / profile-only / negative | completed; fed Phase 098 |
| chunked xyz staging row-source candidate | Phase 097 reflection / Phase 098 evidence | correspondence first；dual-indexed 仅在用户确认 Phase 093 后恢复 | 只暂存当前 chunk 的 x/y/z，可能保留 prematerialized ordered 后段收益，同时避免完整 point cloud materialize 写回。 | 20-run board D/C 为 `0.813x / 0.941x / 0.983x`，退化频率触发 Doctor Error。 | correctness、QEMU、asm、20-run board repeated、Doctor 已完成。 | attempted / profile-only / negative | no production switch |
| bounded production probe after diagnostic mismatch | `.agents` performance-and-ablation 规则、registration row-source evidence 规则 | source-indexed completed and adopted；dual-indexed exact now in Phase 107 patch；correspondence rolled back | diagnostic weak/negative 不直接阻止 production direct；真实 public overload 可能有不同 codegen / baseline。 | production public Std/RVV positive 不能证明新 RVV family 最优；correspondence 已证明这一点：public positive 之后仍需 family A/B。 | PI1-style patch scope、production direct correctness/fallback、production-symbol asm、board repeated、Doctor、必要的 RVV-vs-RVV detail A/B。 | source-indexed adopted; dual adopted for topic-only commit; correspondence not adopted | new bounded phase only |
| production adoption and regression loop | Phase 107 evidence | ordered generic、source-indexed exact、dual-indexed exact、correspondence trial | 接入所有有收益 RVV 实现，负向 correspondence 试接后退回。 | dual 4K 仍有 caveat；generic row-source widening 仍负向。 | correctness、QEMU/asm、board repeated、Doctor、registry、doc-rvv 和 handoff 同步。 | completed / adopted for topic-only commit | topic-only commit preparation; future work needs a new phase |
| phase doc compaction | 用户提交前整理要求 / doc-suite quality bar | `doc/phases/README.zh.md`、`history.zh.md`、Makefile `evidence_status` doc inputs、Handoff | 降低提交文档体量和 reviewer 阅读成本，保留可恢复证据链。 | 不能删除负向 phase 事实，不能让 registry doc-ref 失效。 | `evidence_status` fresh、`git diff --check` pass、artifact tracking scan。 | completed | topic-only commit preparation; future work needs a new phase |
| raw sums fused formula | Phase 010 负向样本 | ordered-cloud-pair | 理论上遍历更少。 | near-cancellation（近抵消）样本下对 reduction tree 过敏。 | 已由 Phase 010 初次 RVV run 暴露问题。 | rejected for now | 只有有误差补偿方案时恢复。 |
| dense finite gate | 当前 `compute3DCentroid` / `demeanPointCloud` 语义审计 | ordered-cloud-pair dense corpus | 首个 diagnostic 收窄到语义清晰且常见的 dense input。 | 非有限 public semantics 不能被改写。 | public semantics tests 和 fallback/gate tests。 | adopted in test-only diagnostic | new PI1 only if a new family appears |
| common RVV reuse audit | common `centroid.hpp` 已有 RVV overload | ordinary cloud API vs iterator path | 判断专用 2D RVV 是否比复用 common RVV 更有价值。 | 2D helper 走 iterator overload，可能无法命中 common ordinary-cloud RVV 路径。 | vectorization report（可选）、bench 分段、asm attribution。 | attempted via asm categories | optional profile only |
| source-indexed carry-over | registration row-source evidence 规则 | source-indexed-cloud-pair | 先物化 source row，再复用同公式 fused math pipeline。 | 64K 接近阈值，收益可能被访存稀释。 | policy-specific correctness、QEMU smoke、asm、5-run board summary、Evidence Doctor。 | attempted / diagnostic-only | `060-production-candidate-review-and-row-source-boundaries` |
| dual-indices carry-over | registration row-source evidence 规则 | dual-indexed-cloud-pair | 双侧物化后复用 fused math pipeline。 | 64K 出现 0.956x 长尾，双 gather / staging 成本不稳定。 | candidate correctness、QEMU smoke、asm、5-run board summary、Evidence Doctor。 | attempted / diagnostic-only | `060-production-candidate-review-and-row-source-boundaries` |
| correspondences carry-over | registration dataflows doc | correspondence-pair | 由 query/match 物化点对，再复用 fused math pipeline。 | 64K 2/5 退化，展开成本不能被 median 小幅正向掩盖。 | public semantics、candidate correctness、QEMU smoke、asm、5-run board summary。 | attempted / diagnostic-only | `060-production-candidate-review-and-row-source-boundaries` |
| topic-local doc suite parity | doc-suite quality bar | README、testing overview、correctness tests、benchmark/evidence、optimization evidence、test-support code map | 让下一轮 worker 和 reviewer 不依赖聊天上下文。 | 后续 evidence / scripts 增长时需刷新。 | doc-suite quality bar 审计和 artifact tracking scan。 | adopted for Phase 050 | refresh if new phase starts |

## 阶段反思新增路线

| phase | new idea | why now | evidence needed | priority |
| --- | --- | --- | --- | --- |
| 000-current-state-and-gaps | fused 2D correlation accumulator | 源码先写 demean matrix，再做 correlation；2D angle 只需要 2x2 `H`。 | correctness、bench、asm、board 和 Evidence Doctor。 | high |
| 000-current-state-and-gaps | finite semantics audit | `compute3DCentroid` 用 `pcl::isFinite`，但 `demeanPointCloud` 全量写出。 | non-finite corpus 和 public semantics tests。 | high |
| 010-scaffold-and-ordered-cloud-pair-correlation-diagnostic | two-pass centered correlation | raw sums 在近抵消样本上对 reduction tree 过敏；中心化两遍更接近 production demean 语义。 | Phase 020 board / asm。 | high |
| 020-board-and-asm-evidence | PI1 production integration plan | Board diagnostic 5-run 为 `weak_positive`，asm 归属和 Evidence Doctor 已闭合到诊断层。 | PI1 fallback matrix、production direct tests、production asm 和 board production evidence plan。 | completed |
| 040-production-integration-plan | exact `PointXYZ` first production gate | generic point type strategy 已读取；Phase 020 只覆盖 `PointXYZ -> PointXYZ`，因此 PI1 选择窄 gate。 | PI3 fallback tests、PI4 production asm 和 board public repeated summary。 | completed / archived |
| 050-pi2-production-patch-and-direct-evidence | production candidate supported | 最新 production-public board repeated 为 4.222x / 5.310x / 4.947x，Doctor 0/0/0。 | 保留窄范围 patch，刷新 evaluation、matrix、roadmap 和 Handoff。 | completed / current |
| 050-pi2-production-patch-and-direct-evidence | row-source family carry-over remains separate | ordered-cloud-pair 的 production-public 证据只覆盖 exact ordered row source，不能把其它 row source 写成 production follow-up。 | 新 phase 只在 test-rvv 范围补 policy-specific diagnostic。 | medium |
| 030-row-source-family-carryover | materialize-to-ordered bounded diagnostic | 三类 row source 的展开成本必须进入 bench，先把 row pairing 与数学 candidate 分开验证。 | board repeated 可用后决定是否继续 gather / staging 设计。 | medium |
| 090-row-source-direct-gather-staging-expansion | direct gather 更适合 source-indexed | source-indexed direct gather 三个规模稳定正向且无本 row source Doctor Error。 | 新建有界 production probe，补 public correctness/fallback、production-symbol asm、board repeated、Doctor。 | high |
| 090-row-source-direct-gather-staging-expansion | dual/correspondence 不能直接否决 production probe | direct diagnostic 在 dual/correspondence 上 negative，但 diagnostic baseline 与 public scalar boundary 不同。 | 先做 mismatch audit；若进入 probe，必须限界、补停止条件和同 production boundary RVV-vs-RVV A/B。 | medium |
| 091-row-source-bounded-production-probe | source-indexed public boundary 与 diagnostic boundary 差异很大 | source-indexed public probe 在生产公开入口上达到 4.103x / 4.818x / 4.575x，比 Phase 090 diagnostic 结果更强，用户已确认采纳。 | 若要声明实现族最优，再补同 production boundary RVV-vs-RVV family A/B。 | high |
| 092-source-indexed-family-ab | current direct gather family 可保留，但只是弱正向 | 同一 RVV binary 内，materialize+ordered public 相对 direct source-indexed public 的 B/A 为 1.089x / 1.044x / 1.037x，Doctor 0/0/2。 | 后续不要为 source-indexed 更换 production dispatch；dual-indexed / correspondence 仍按独立 row source 阶段处理。 | high completed |
| 093-dual-indexed-family-ab | dual-indexed direct gather 在真实 public boundary 内胜出 | 同一 RVV binary 内，materialize+ordered public 相对 direct dual-indexed public 的 B/A 为 1.018x / 1.671x / 1.554x，Doctor 0/3/1；Warning 全在 4K。 | Phase 107 已把 exact dual-indexed 接入当前 patch；当前由 Phase 109 20-run 刷新为 1.080x / 1.661x / 1.646x，仍保留 4K caveat。 | high completed / adopted for topic-only commit |
| 094-correspondence-guarded-probe-or-staging-profile | public probe 正向但 family A/B 不干净 | public Std/RVV 为 4.517x / 3.705x / 3.149x，说明真实公开入口 RVV 快于 public scalar；但同 boundary family A/B 的 256K 有 5/20 低于 1，Doctor 报 Error。 | Phase 107 试接入后 family A/B 仍为 negative，correspondence production dispatch 已退回；后续只允许新的 bounded candidate。 | high completed / rolled back in Phase 107 |
| 095-correspondence-staging-profile-or-long-tail-mitigation | staged-dual 没有缓解 correspondence 长尾 | direct/staged D/S 为 0.833x / 0.729x / 0.848x，Doctor 3/2/0；staged-dual 比 direct 慢，copy + dual public 路径不能作为生产替代路线。 | 不切换 dispatch；Phase 107 correspondence exact 也已退回。 | high completed / negative |
| 096-correspondence-locality-order-profile | locality/order 没有支持 staged-dual switch | identity / reverse 中 direct 稳定快于 staged-dual；shuffled 中 materialize 明显更快；strided 有长尾但 overall 仍 negative，Doctor 12/5/0。 | 默认转 component ablation；若之后要尝试 chunked xyz staging，必须先有 component ablation 证据。 | high completed / negative |
| 097-correspondence-component-ablation | component no-solve 提供成本线索，但 full-anchor 仍为 negative | strided full-anchor D/S 为 0.834x / 0.761x / 0.841x，Doctor 3/2/0；prematerialized ordered full 快，但完整 materialize 写回很贵。 | 不切 production dispatch；若继续技术探索，下一步只能新建 chunked xyz staging candidate phase 并重新证据化。 | high completed / negative |
| 098-correspondence-chunked-xyz-staging-candidate | chunked xyz staging 没有形成 correspondence 替代路线 | 20-run board D/C 为 0.813x / 0.941x / 0.983x，Doctor 3/3/0；QEMU/asm/correctness 均闭合但性能方向负向。 | 不切 production dispatch；已转 Phase 099 source-indexed generic point-type expansion。 | high completed / negative |
| 099-source-indexed-generic-xyz-point-type-expansion | source-indexed 泛型点型不能直接从 091 exact gate 继承 | 代表性 16-case board 为 mixed-negative，Doctor 5/10/1；QEMU/asm/correctness 均闭合。 | 不切 source-indexed production dispatch；下一步独立审计 dual-indexed generic point types。 | high completed / mixed-negative |
| 100-dual-indexed-generic-xyz-point-type-expansion | dual-indexed 泛型点型不能直接从 093 exact gate 继承 | 代表性 16-case board 为 negative，Doctor 13/17/1；QEMU/asm/correctness 均闭合。 | 不切 dual-indexed production dispatch；下一步独立审计 correspondence generic point types。 | high completed / negative |
| 101-correspondence-generic-xyz-point-type-expansion | correspondence 泛型点型不能直接从 094 guarded exact gate 继承 | 代表性 16-case board 为 negative，Doctor 11/19/2；QEMU/asm/correctness 均闭合。 | 不切 correspondence generic production dispatch；默认恢复转用户检查点或新有界候选。 | high completed / negative |
| 102-production-decision-package | 用户已采纳 Phase 091，需要把 091/093/094 和 099/100/101 的证据状态拆清 | Phase 091 public 正向且采纳；Phase 093 positive / pending；Phase 094 public positive but family A/B unstable；generic row-source diagnostics negative。 | 停在用户决策检查点；后续 production 变化必须另开有界 PI1-PI5。 | high completed / checkpoint |
| 103-source-indexed-generic-bounded-production-probe | source-indexed generic public probe 比 Phase 099 diagnostic 更接近真实 production boundary | 16-case public board 15/16 positive，但 `PointNormal->PointNormal 256K` 有 below-1 和 Doctor Error。 | 单 case long-tail rerun 已由 Phase 104 完成；完整代表性方差仍需独立 20-run evidence。 | high completed / guarded |
| 104-source-indexed-generic-pointnormal-256k-long-tail-diagnostic | Phase 103 唯一 below-1 case 单独复跑转 positive | 5-run median `2.879x`、`B/A<1=0/5`，但 Doctor 仍有 variance Warning。 | 不能单凭单 case 复跑 clean-adopt；下一步是 Phase 106 代表性 20-run variance。 | high completed / guarded |
| 105-evidence-freshness-and-recovery-sync | current handoff YAML、phase README、roadmap 和 matrix 需要从旧用户检查点恢复到继续证据化 | `evidence_status` 已 fresh；Phase 103/104 仍 guarded，Phase 091 保持 adopted。 | Phase 106 必须使用独立 board label / evidence dir，完成后停在用户决策点。 | high completed / recovery synced |
| 106-source-indexed-generic-public-variance | 代表性 20-run 方差证据推翻了“全泛型 clean positive”假设 | 独立 board dir 的 16-case summary 为 12 positive、1 weak_positive、3 negative；`PointNormal->PointNormal 256K` 为 `7/20` below-1，Doctor `1/27/0`。 | 不 clean-adopt source-indexed generic widening；若继续，必须由用户选择收窄范围、追查负向 case 或另开 bounded candidate。 | high completed / negative guarded |
| 109-dual-indexed-exact-public-variance | 已接入 dual-indexed exact 的 4K caveat 得到更严格方差复核 | Phase 109 20-run 为 4K/64K/256K `1.080x / 1.661x / 1.646x`；4K `1/20` below-1，64K/256K `0/20` below-1；Doctor `0/3/0`。 | 保留 exact dual-indexed dispatch 和 4K caveat；下一步若继续，应转 source-indexed generic negative-case investigation 或 correspondence new bounded candidate。 | high completed / positive with caveat |
| 110-source-indexed-pointxyzi-exact-public-probe | Phase 106 中 PointXYZI 子范围独立为正，值得和 full generic negative 分开证据化 | Phase 110 20-run 为 4K/64K/256K `3.979x / 3.547x / 3.602x`；三个规模 `B/A<1=0/20`；Doctor `0/3/0`。 | 正向只覆盖 exact `PointXYZI -> PointXYZI` source-indexed public probe，不扩大 source-indexed generic widening。 | high completed / adopted by Phase 112 |
| 111-source-indexed-normal-negative-case-investigation | Phase 106 full generic negative 需要和 Phase 110 PointXYZI exact positive 拆清 | Normal 类 48B stride case 中，`PointNormal->PointNormal 256K` `7/20` below-1，另有两个 Normal 类 case 出现 `1/20` below-1；Doctor `1/27/0` 仍有效。 | Normal 类 source-indexed generic widening 不接入；若未来继续，必须另开独立 exact Normal probe 或 profiling phase。 | high completed / rejected for current gate |
| 111-source-indexed-normal-negative-case-investigation | 用户不再推进 Normal 类后续优化 | 可能的 load2、exact Normal specialization、selected `x/y` materialization 和 chunk/profile ablation 只解释“还有可尝试的形态”，不再进入默认 phase loop。 | 保留在 roadmap 和 matrix 作为 not-planned 候选；除非用户明确重开，否则不创建新的 Normal probe。 | completed / not planned |
| 112-source-indexed-pointxyzi-adoption-closeout | 用户确认采纳 Phase 110 exact PointXYZI 正向 probe，需要把 probe 状态转为 production truth | Phase 110 correctness/QEMU/asm/board evidence 全部闭合；Phase 112 只做 adoption closeout 和提交前验证。 | adopted 只覆盖 exact `PointXYZI -> PointXYZI`，Phase 103/104/106 generic 和 Phase 111 Normal 仍不采纳。 | high completed / adoption closeout |
| 113-doc-rvv-closeout-and-log-hygiene | 用户指出 production `doc-rvv` closeout 被执行成小型 freshness sync，且 tracked logs 复跑后继续脏工作区 | `doc-rvv` 必须重构为长期生产维护文档；tracked generated logs 需要从 Git 索引移除并由 ignore 规则接管。 | 已完成并提交为 `4ba8e63fd`；不改 production source，不提交 raw/generated log additions。 | high completed / committed |
| 114-test-support-responsibility-split-and-agent-rule-update | 用户指出测试支撑拆分应职责优先，不能等到 800/1000 行阈值才触发 | TE2D 测试支撑多职责 helper 应拆到 `include/impl/te2d_*.hpp`；`.agents` rule update 已作为 Commit A 单独完成。 | correctness/QEMU smoke、`evidence_status`、YAML parse、旧入口扫描和 diff check 已通过；Commit B 只提交 TE2D 测试支撑拆分与 topic-local 文档同步。 | high completed / ready for Commit B |

## 暂缓 / 拒绝路线

| candidate family | reason | resume condition |
| --- | --- | --- |
| broadening production patch | ordered-cloud-pair 当前正向，但 indexed / correspondence 仍有 64K 不稳定证据。 | 保留 exact gate，禁止自动扩大 row source / 点型 / Scalar；新边界必须独立 PI1。 |
| raw sums fused formula | Phase 010 近抵消样本失败，说明它对 reduction tree 过敏。 | 有补偿求和、double accumulator 或其它误差预算证据时再恢复。 |
| indexed / correspondences production | source-indexed 已有 adopted narrow production patch；dual-indexed exact 已进入 Phase 107 当前 patch 并按用户授权纳入 topic-only commit；correspondence public probe 曾 positive，但 Phase 107 family A/B negative，且 Phase 095 staged-dual、Phase 096 locality/order、Phase 097 component ablation 和 Phase 098 chunked staging 均为 negative。 | correspondence 只有新的 bounded candidate 才能恢复；dual-indexed 当前只覆盖 exact `PointXYZ -> PointXYZ`，不能直接扩大为 generic row-source。 |
| correspondence staged-dual / chunked staging dispatch switch | Phase 095 direct/staged D/S 三个规模均为 negative，Board Doctor `3/2/0`；Phase 096 四类 locality/order profile overall negative，Board Doctor `12/5/0`；Phase 097 full-anchor 仍为 negative，Board Doctor `3/2/0`；Phase 098 chunked D/C 三个规模均为 negative，Board Doctor `3/3/0`。 | 只有新的同边界 candidate 找到可复现正向形态，并重新完成 production PI2-PI5 后才恢复。 |
| source-indexed generic point-type production dispatch | Phase 099 source-indexed representative diagnostic 为 mixed-negative，Board Doctor `5/10/1`。 | 只有用户明确要求 bounded production probe，或后续找到能解释 091/099 boundary mismatch 的 production 形态，并重新完成 PI1-PI5 后才恢复。 |
| source-indexed generic public widening clean adoption | Phase 106 independent 20-run public variance 为 12 positive、1 weak_positive、3 negative，Board Doctor `1/27/0`；`PointNormal->PointNormal 256K` 有 `7/20` below-1。 | 只有用户明确要求收窄 adopted 范围、修复/解释负向 case 并重新补同边界 public variance evidence，或授权回滚/继续新候选时才恢复。 |
| source-indexed Normal exact/generic production dispatch | Phase 111 审计显示 Normal 类 48B stride 与 source-indexed gather / target strided load 组合下仍有 below-1 和长尾；不能继承 Phase 110 `PointXYZI` exact 正向证据。 | 只有新的 exact Normal bounded probe 或 profiling phase 证明无 below-1 / Doctor Error，且用户授权进入 PI1-PI5 时才恢复。 |
| source-indexed PointXYZI generic widening from exact evidence | Phase 112 只采纳 exact `PointXYZI -> PointXYZI`；Phase 106 full representative widening 仍 negative。 | 只有独立 generic / representative evidence 重新证明无 negative bucket 和 Doctor Error 时才恢复。 |
| dual-indexed generic point-type production dispatch | Phase 100 dual-indexed representative diagnostic 为 negative，Board Doctor `13/17/1`。 | 只有用户明确要求 bounded production probe，或后续找到能解释 093/100 boundary mismatch 的 production 形态，并重新完成 PI1-PI5 后才恢复。 |
| correspondence generic point-type production dispatch | Phase 101 correspondence representative diagnostic 为 negative，Board Doctor `11/19/2`。 | 只有用户明确要求 bounded production probe，或后续找到能解释 094/101 boundary mismatch 的 production 形态，并重新完成 PI1-PI5 后才恢复。 |
| `Scalar=double` RVV | 当前候选和板卡计划只覆盖 float。double reduction 和 trig output 需要独立预算。 | double-specific correctness、asm 和目标硬件证据。 |
| generic point type production broadening beyond representative cases | Phase 080 已完成 representative public direct evidence，但 board 有一个 negative bucket 和多项 variance warning。 | 当前 patch 已采纳；不以其它 case 覆盖 negative，未逐类型上板的自定义点型继续排除。 |

## 默认恢复动作

`roadmap_default_recovery_queue`：

1. `commit-b-review`：Phase 114 通过后默认停在 reviewer 检查 Commit B；不自动创建新的 RVV 性能优化 phase。
3. `source-indexed-normal-exact-probe-or-profiling-only-by-user-request`：Phase 111 已关闭当前 Normal 类负向审计；没有新授权时不继续接入 Normal。若用户明确要求继续，需新建独立 exact Normal probe 或 profiling phase。
4. `correspondence-new-bounded-candidate-only`：correspondence direct/staged/component/chunked/generic 现有证据均不支持接入；只有新的同边界 candidate、独立 phase plan 和完整 correctness/QEMU/asm/board/Doctor 后才恢复。
5. `point-type-inventory-expansion`：如需把未逐类型上板的 traits-allowed 内建点型或自定义点型写成已覆盖范围，必须另建点型 inventory / bench phase。
6. `row-source-production-integration-plan`：只有具体 row source 的 production probe 或 policy-specific candidate 在 correctness、fallback、asm、board repeated 和 Doctor 上成立，才进入 PI1-PI5；不能 clean-adopt 未做 family comparison 的新实现族。
7. `dual-indexed-generic-production-probe-deferred`：Phase 100 negative 阻止默认 production widening；只有另开生产接入阶段并重新补接入后 correctness、性能、asm 和 Evidence Doctor 才能改变 dispatch。
8. `correspondence-generic-production-probe-deferred`：Phase 101 negative 阻止默认 production widening；只有另开生产接入阶段并重新补接入后 correctness、性能、asm 和 Evidence Doctor 才能改变 dispatch。

QEMU timing 始终不能替代板卡性能结论。
