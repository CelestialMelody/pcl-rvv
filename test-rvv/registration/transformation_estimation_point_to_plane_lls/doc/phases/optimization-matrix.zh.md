# Optimization Matrix

本矩阵只记录当前 topic 的候选族、行来源策略和证据边界。`planned`、`deferred`
和 `historical diagnostic` 不能写成 adopted；QEMU timing 不作为性能结论。

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| fused-formula block-reduction production dispatch | full-cloud | source xyz f32 AoS, target xyz+normal f32 AoS, `Scalar=float`; representative board: `PointNormal -> PointNormal`, `PointXYZ -> PointNormal`, `PointXYZ -> PointXYZINormal` | real public full-cloud overload; RVV gate then `ConstCloudIterator` fallback | `run_test_compare`; production direct `ProductionFullCloud*`, generic point tests, small input / layout / `Scalar=double` fallback | `run_bench_compare --case-filter production-dispatch` as log-shape / board input | `output/board/production_dispatch_generic_representative_5run_summary.md`, 5-run positive | helper-level and production-symbol asm recorded in evaluation | manual doctor from summary; metadata incomplete because no JSON manifest | production-candidate / adopted within stated boundary | 当前阶段只验证 reference 边界清理没有破坏 correctness；不改 hot path。 |
| current block-reduction baseline | full-cloud | `PointNormal -> PointNormal`, historical production-shaped baseline | no longer production selector; kept in `include/impl/teptpl_reductions.hpp` / historical summaries | historical tests and diagnostic wrappers | block / current baseline case | historical values inside `production_dispatch_generic_representative_5run_summary.md` and `block_fused_formula_5run_summary.md` | historical asm only | manual doctor / metadata incomplete | historical baseline, not adopted production selector | none unless reviewer requests new same-boundary A/B. |
| staging / compressed-tail family | source-indexed | `PointNormal`, `Scalar=float`, valid-index-only diagnostic | test-rvv diagnostic only | row-source diagnostic tests | historical row-source bench | historical / not current production evidence | missing current production asm | not current | no-production / historical diagnostic | separate follow-up only; do not inherit full-cloud production result. |
| dual-indices diagnostic family | dual-indices | `PointNormal`, `Scalar=float`, valid-index-only diagnostic | test-rvv diagnostic only | row-source diagnostic tests | historical row-source bench | historical negative / distribution-sensitive | missing production asm | not current | no-production / historical diagnostic | separate profile or component ablation topic. |
| correspondences diagnostic family | correspondences | `PointNormal`, `Scalar=float`, valid-index-only diagnostic | test-rvv diagnostic only; query/match expansion included in diagnostic boundary | row-source diagnostic tests | historical correspondence bench | historical negative / distribution-sensitive | missing production asm | not current | no-production / historical diagnostic | separate profile or component ablation topic. |
| reference / production-detail boundary cleanup | full-cloud | same as adopted production candidate; tests use test-only scalar reference for expected normal equation | production header removes non-runtime scalar normal-equation reference; tests convert RVV production equation into test-support equation | `git diff --check` clean; `run_test_std` 40/40 passed; `run_test_rvv` 40/40 passed | not applicable; no hot path or bench case change | not required because only reference/test-support boundary changed | not required for this cleanup | manual doctor: correctness-only, no new performance data; registry not available | done / ready_for_review | none inside this phase. |
| test harness layout migration | test support architecture | same production candidate boundary; layout-only test asset change | Phase 010 moved test / bench sources to `src/`, added `include/teptpl.h`; old alias later removed in Phase 030 | `git diff --check` clean; `run_test_std` 40/40 passed; `run_test_rvv` 40/40 passed | std bench compile smoke and RVV bench compile / asm smoke; no QEMU timing performance conclusion | not required because source layout does not change hot path, bench case, case filter or dispatch gate | asm dump regenerated by compile smoke, but not used as new hotspot attribution | manual doctor: no new benchmark / board summary; registry added later in Phase 030 | done / superseded by Phase 050 structure layout | source split and internal helper layout are no longer optional follow-up; see Phase 050 row. |
| roadmap / evaluation recovery | documentation and recovery | same production candidate boundary; documentation-only change | added topic-level optimization roadmap; moved evaluation main ownership to `doc/`; old root pointer later removed in Phase 030 | `git diff --check` clean; reference check passed; `run_test_std` 40/40 passed; `run_test_rvv` 40/40 passed | not applicable; no bench or hot path change | not required | manual doctor: no new benchmark / board summary; registry added later in Phase 030 | done / ready_for_review | legacy cleanup and evidence registry adoption handled by Phase 030. |
| legacy cleanup / evidence registry adoption | documentation and evidence freshness | same production candidate boundary; no algorithm change | remove root evaluation legacy pointer and old test support alias; add `log/evidence_registry.json` for current summary/QEMU evidence | `rg` dependency check; `git diff --check`; `run_test_std`; `run_test_rvv` | not applicable; no bench or hot path change | not required | not changed; no new asm attribution | registry check with doc refs; no new Evidence Doctor manifest because no new benchmark/summary generated | done / ready_for_review | none inside this phase; future registry freshness checks only. |
| doc suite parity | documentation and structure maturity | same production candidate boundary; no algorithm change | add topic-local README, testing overview, correctness tests, benchmark/evidence, optimization evidence and test-support code map; split reader paths out of evaluation/topic doc | `git diff --check`; registry check fresh; `run_test_std` / `run_test_rvv` 40/40 via temp logs under `/tmp` | not applicable; no bench or hot path change | not required | not changed; no new asm attribution | manual doctor: documentation-only, no new benchmark/summary generated; registry check fresh | done / superseded by Phase 050 validity check | Phase 040 的 `050-test-source-split` 停止位已由 Phase 050 重新恢复并闭合；当前不再作为 deferred 队列项。 |
| structure layout split | test source and internal helper layout | same production candidate boundary; no algorithm change | split gtest into four `src/test_teptpl_*.cpp`; keep `src/bench_teptpl.cpp` as thin entry; move old internal helpers to `include/impl/teptpl_*.hpp`; add `include/test_teptpl.h` and `include/bench_teptpl.h` | `run_test_std` 40/40 via `/tmp/teptpl_phase050_run_test_std.log`; `run_test_rvv` 40/40 via `/tmp/teptpl_phase050_run_test_rvv.log` | std bench compile smoke; `dump_bench_rvv` compile / asm smoke; no performance conclusion | not required because layout-only change does not change production hot path, bench labels or dispatch gate | asm dump regenerated by compile smoke only | manual doctor: layout-only, no new benchmark/board summary; registry check required after doc sync | done / adopted | none for test-source-split or internal-helper-layout; remaining candidates require production or evidence scope expansion. |
