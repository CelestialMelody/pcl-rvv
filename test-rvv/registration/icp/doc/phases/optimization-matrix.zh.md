# ICP optimization matrix

| family | row source | point type / Scalar / layout | correctness | bench backend | board performance | asm | Evidence Doctor | decision |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `xyz-masked-store-m2` | full-cloud | `PointXYZ` / `Scalar=float` / AoS offsets | QEMU Std/RVV 12 tests passed；board RVV 12 tests passed | QEMU bench compare guarded；performance only on board | production direct median 5.68x (64K), 5.30x (256K) | production `IterativeClosestPoint<PointXYZ>::transformCloud` symbol has m2/FMA/masked-store cluster；Std fallback has `transformCloudStandard` symbol | Errors=0；`PointXYZ 64K` long-tail / group-outlier warnings documented | adopted |
| `xyz-normal-two-mask-m2` | full-cloud | `PointNormal` / `Scalar=float` / AoS offsets | QEMU Std/RVV 12 tests passed；board RVV 12 tests passed | QEMU bench compare guarded；performance only on board | production direct median 3.76x (64K), 3.93x (256K) | production `IterativeClosestPoint<PointNormal>::transformCloud` symbol has XYZ+normal m2/FMA/two-mask/masked-store clusters；Std fallback has `transformCloudStandard` symbol | Errors=0；no normal-specific warnings | adopted |
| production generic layout gate | full-cloud | template `PointSource` / `Scalar=float` / registered float XYZ or XYZ+normal AoS layout | `PointXYZI` and `PointXYZINormal` production direct tests passed；small input and `Scalar=double` fallback passed | representative board bench uses `PointXYZ` / `PointNormal` | inherited from representative production direct board evidence | traits + runtime offset gate documented | Doctor clean except accepted `PointXYZ 64K` warnings | adopted with representative performance |
| QEMU bench compare | full-cloud smoke only | historical `qemu_smoke_only` | not used for performance | default guard blocks `run_bench_compare` unless `ALLOW_QEMU_BENCH_COMPARE=1` | not_applicable | not_applicable | historical only | removed from default workflow |
| doc-suite parity | documentation structure | README + topic-local docs + evaluation + long-term `doc-rvv` | not_applicable | not_applicable | not_applicable | not_applicable | not_applicable | adopted in Phase 004 |
| `IterativeClosestPointWithNormals` path | not_applicable | override calls `pcl::transformPointCloudWithNormals` | not_applicable | not_applicable | not_applicable | not_applicable | not_applicable | separate topic if needed |
