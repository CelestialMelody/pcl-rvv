# min_cut_segmentation 优化矩阵

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| unary foreground min-distance batch | input-indexed point rows | `PointXYZ` / float xyz / dense AoS | test-only component helper mirroring `calculateUnaryPotential` sink weight | `run_test_compare` passed | component case `unary_min_distance` | `log/board/repeated/summary.md`: 5-run median 2.08x, positive | `dump_bench_rvv` shows `computeUnaryPotentialsRVV` RVV instructions | `log/board/repeated/evidence_doctor.md`: 0 / 0 / 0 | diagnostic-positive | feed Phase 010 |
| binary distance + expf approximation | edge list source/target index pairs | `PointXYZ` / float xyz / dense AoS | test-only component helper mirroring `calculateBinaryPotential` within float tolerance | `run_test_compare` passed | component case `binary_exp_weight` | `log/board/repeated/summary.md`: 5-run median 2.44x, positive | `dump_bench_rvv` shows `computeBinaryPotentialsRVV` and `expf_RVV_f32m2` RVV instructions | `log/board/repeated/evidence_doctor.md`: 0 / 0 / 0 | diagnostic-positive | feed Phase 010 |
| production-shaped buildGraph timing | public-like MinCutSegmentation input | `PointXYZ` / float xyz / dense AoS + Boost graph | test-only buildGraph-shaped diagnostic, no production dispatch | `run_test_compare` passed 3 tests | case `buildgraph_potential_batch` | `log/board/buildgraph-repeated/summary.md`: 5-run median 1.02x, min 0.98x, neutral | `dump_bench_rvv` shows RVV helper calls and vector instructions | `log/board/buildgraph-repeated/evidence_doctor.md`: Errors=1 / Warnings=0 / Suggestions=1 | no-production | stop current potential-batch line |
