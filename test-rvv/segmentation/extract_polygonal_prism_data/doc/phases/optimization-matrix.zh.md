# extract_polygonal_prism_data Optimization Matrix

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| scalar reference scaffold | dense ordered indices | `PointXYZ` / `float` / horizontal projection | test helper after projection | `make run_test_std` pass，2 tests | QEMU bench smoke only | not_applicable | not_applicable | not_applicable | adopted for diagnostic baseline | none |
| RVV edge-parity candidate | dense ordered indices | `PointXYZ` / `float` / horizontal projection / single polygon | `make run_test_rvv` pass；multi-polygon fallback test pass | historical post-projection scan；QEMU smoke checks log shape only | historical：5 runs，median 3.56x，min 3.53x，max 3.65x | historical asm under `segmentPolygonalPrismRvvCandidate` | historical Evidence Doctor 0 / 0 / 0 | superseded by full-scan diagnostic | none |
| RVV full-scan diagnostic | dense ordered indices | `PointXYZ` / `float` / arbitrary plane coefficients / runtime `k1/k2` / single polygon | `make run_test_compare` pass；倾斜平面 full-scan case pass | `bench_eppd` full-scan diagnostic；QEMU checksum `17458065235267028439` 一致 | `log/board/repeated/summary.md`：5 runs，median 1.98x，min 1.77x，max 2.16x | `build/asm/riscv/bench_eppd_rvv.full.asm` shows `vlse32.v`、`vfmacc.vf`、`vmand.mm`、`vmxor.mm`、`vcompress.vm` | `log/board/repeated/evidence_doctor.md`：0 / 0 / 0 | production-shaped diagnostic positive | fed Phase 040 production public |
| arbitrary indices diagnostic | source indexed points | `PointXYZ` / `float` / gather / 32-bit byte offset | `make run_test_compare` pass；`FullScanIndexedPathKeepsSourceIndices` 命中 `RvvIndexed` | `bench_eppd --indices indexed`；QEMU smoke checksum `11874477441850024409` 一致 | `log/board/repeated-indexed/summary.md`：5 runs，median 2.20x，min 1.97x，max 2.32x | `build/asm/riscv/bench_eppd_rvv.full.asm` shows indexed gather and `vcompress.vm` | `log/board/repeated-indexed/evidence_doctor.md`：0 / 0 / 0 | production-shaped diagnostic positive | fed Phase 040 production public |
| production public RVV scan | dense ordered indices | `RVVXYZAoSFloatLayout<PointT>` / `float` distance / single polygon / `indices_->size() >= 32` | public `ExtractPolygonalPrismData<PointT>::segment` | `make run_test_compare` pass；`make run_board_test` pass；small input fallback pass | `bench_eppd --path production --indices dense`；QEMU smoke checksum一致 | `log/board/repeated-production/summary.md`：5 runs，median 1.75x；Phase 070 `log/board/repeated-production-size32-threshold32-confirm5/summary.md`：5 runs，median 1.19x，min 1.18x | production `segmentRvv` symbol shows `vlse32.v`、`vfmacc.vf`、`vmxor.mm`、`vcompress.vm` | production dense doctor 0 / 0 / 0；threshold32 confirm5 doctor 0 / 0 / 0 | adopted production behavior | none |
| production public RVV scan | source indexed points | `RVVXYZAoSFloatLayout<PointT>` / `float` distance / indexed gather / single polygon / `indices_->size() >= 32` | public `ExtractPolygonalPrismData<PointT>::segment` | `make run_test_compare` pass；`make run_board_test` pass；indexed direct test matches `segmentStd` | `bench_eppd --path production --indices indexed`；QEMU smoke checksum一致 | `log/board/repeated-production-indexed/summary.md`：5 runs，median 1.75x，min 1.72x，max 1.75x | production `segmentRvv` symbol shows `vluxseg3ei32.v` and `vcompress.vm` | `log/board/repeated-production-indexed/evidence_doctor.md`：0 / 0 / 0 | adopted production behavior | none |
| concave hull XOR RVV | dense ordered indices | `RVVXYZAoSFloatLayout<PointT>` / `float` distance / nested polygons / `indices_->size() >= 32` | public `segment` multi polygon path | `SegmentRvvMatchesSegmentStdForNestedPolygons`；`make run_test_compare` pass | `bench_eppd --path production --polygons nested`；QEMU checksum `7795032464688786530` 一致 | `log/board/repeated-production-nested/summary.md`：5 runs，median 2.18x，min 2.11x，max 2.23x | production `segmentRvv` symbol shows mask XOR and `vcompress.vm` | `log/board/repeated-production-nested/evidence_doctor.md`：0 / 0 / 0 | adopted production behavior | none |
| concave hull XOR RVV | source indexed points | `RVVXYZAoSFloatLayout<PointT>` / `float` distance / indexed gather / nested polygons / `indices_->size() >= 32` | public `segment` multi polygon path | `SegmentRvvMatchesSegmentStdForIndexedNestedPolygons`；`make run_test_compare` pass | `bench_eppd --path production --indices indexed --polygons nested`；QEMU checksum `5040983731874815810` 一致 | `log/board/repeated-production-nested-indexed/summary.md`：5 runs，median 2.13x，min 2.11x，max 2.17x | production `segmentRvv` symbol shows gather, mask XOR and `vcompress.vm` | `log/board/repeated-production-nested-indexed/evidence_doctor.md`：0 / 0 / 0 | adopted production behavior | none |
| production fallback | `<32` small input / degenerate polygon / invalid indices / unsupported layout | template `PointT` fallback to `segmentStd` | public `segment` | `SegmentRvvDeclinesSmallInputs`、`SegmentRvvDeclinesDegeneratePolygons`、Std build pass | not_applicable | board correctness pass via `make run_board_test` | not_applicable | covered by correctness, no separate doctor | adopted fallback behavior | reviewer should inspect init/deinit fallback state |
| point type expansion | dense ordered indices | `PointXYZI` / `float` / `sizeof(PointT) <= 32` / single polygon | public `segment` | `SegmentRvvMatchesSegmentStdForPointXYZI`；`make run_test_compare` pass | `bench_eppd --path production --point-type xyzi` | `log/board/repeated-production-xyzi-postgate/summary.md`：5 runs，median 1.85x，min 1.77x，max 1.87x | production `segmentRvv` shared boundary | `log/board/repeated-production-xyzi-postgate/evidence_doctor.md`：0 / 0 / 0 | adopted production behavior | none |
| point type expansion | dense ordered indices | `PointXYZRGB` / `float` / `sizeof(PointT) <= 32` / single polygon | public `segment` | `SegmentRvvMatchesSegmentStdForPointXYZRGB`；`make run_test_compare` pass | `bench_eppd --path production --point-type xyzrgb` | `log/board/repeated-production-xyzrgb-postgate/summary.md`：5 runs，median 1.83x，min 1.75x，max 1.88x | production `segmentRvv` shared boundary | `log/board/repeated-production-xyzrgb-postgate/evidence_doctor.md`：0 / 0 / 0 | adopted production behavior | none |
| point type expansion | dense ordered indices | `PointXYZRGBA` / `float` / `sizeof(PointT) <= 32` / single polygon | public `segment` | `SegmentRvvMatchesSegmentStdForPointXYZRGBA`；`make run_test_compare` pass | `bench_eppd --path production --point-type xyzrgba` | `log/board/repeated-production-xyzrgba-postgate/summary.md`：5 runs，median 1.84x，min 1.79x，max 1.86x | production `segmentRvv` shared boundary | `log/board/repeated-production-xyzrgba-postgate/evidence_doctor.md`：0 / 0 / 0 | adopted production behavior | none |
| wide-stride point type | dense ordered indices | `PointXYZINormal` / `sizeof(PointT) > 32` / single polygon | public `segment` fallback | `SegmentRvvDeclinesPointXYZINormal`；QEMU fallback smoke checksum matches | `bench_eppd --path production --point-type xyzinormal` | `log/board/repeated-production-xyzinormal-postgate/summary.md`：5 runs，median 1.00x，min 0.99x，max 1.03x | RVV path intentionally not entered | `log/board/repeated-production-xyzinormal-postgate/evidence_doctor.md`：0 / 1 / 1；historical 20-run doctor 1 / 1 / 0 | rejected for RVV, adopted scalar fallback | only revisit in a dedicated wide-stride phase |
| size threshold tuning | dense ordered indices | `PointXYZ` / single polygon / production path | public `segment` | `SegmentRvvDeclinesSmallInputs` now covers `<32`; `make run_test_compare` pass | `bench_eppd --path production --size <N> --iterations 32 --warmup 4` | `log/board/repeated-production-size32-threshold32-confirm5/summary.md`：5 runs，median 1.19x，min 1.18x，max 1.38x；3-run sweep 32..256 all positive but low-run-count | production `segmentRvv` shared boundary | confirm5 doctor 0 / 0 / 0；3-run sweep doctor 0 / 1 / 0 | adopted threshold 32 | none; wider changes require separate topic / phase authorization |
