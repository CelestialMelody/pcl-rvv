# GrabCut 优化矩阵

| candidate family | row source policy | point type / Scalar / layout | scope and entry | correctness / fallback target | bench / ablation target | board evidence | asm boundary | Evidence Doctor | decision | unblocked next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| organized n-link scalar reference | organized image scan | `PointXYZRGB` / float color / organized cloud | test-only reference for `computeBetaOrganized` + `computeNLinksOrganized` formula | `make run_test_compare` passed | `bench_grabcut --case organized_nlinks` exists | historical all-case board observation: `320x240` was about `0.98x` | `dump_bench_rvv` generated RVV binary asm, but hot benefit not established | all-case doctor previously flagged organized n-link `ba_degradation_frequency`; current board manifest is GMM-only | attempted / weak-negative diagnostic | Do not prioritize production. Reopen only with repeated board and memory / asm attribution plan. |
| RVV organized n-link candidate | organized image scan | `PointXYZRGB` / float color / organized cloud | pre-production diagnostic; no production dispatch | RVV gtest passed with tolerance | component A/B exists | historical all-case board observation was weak-negative | asm exists but benefit attribution weak | historical all-case doctor produced an Error for `B/A < 1` | attempted / not promoted | Keep as diagnostic asset; do not use as PI1 trigger. |
| non-organized KNN path | KNN neighbor search | indexed non-organized cloud | `computeBetaNonOrganized` + `computeNLinksNonOrganized` | not_applicable with evidence | deferred | not_applicable | not_applicable | not_applicable | deferred | Reopen only if organized diagnostic or profile shows KNN path is not search-dominated. |
| GMM probability density | synthetic pixel samples | single Gaussian / float BGR / finite domain | test-only batch wrapper for `GMM::probabilityDensity(i, c)` in `segmentation/src/grabcut_segmentation.cpp` | `make run_test_compare` passed; max error within `2e-5 / 2e-4` budget | `bench_grabcut --case gmm_probability` | Milkv-Jupiter `640x480`, `iterations=8`, `warmup=2`: Std `22.013766 ms`, RVV `13.026312 ms`, `1.69x` | `computeGMMProbabilityCandidate` contains `vle32.v`, `vse32.v`, `vfmacc.vf`, `vfmacc.vv` | `doc/phases/010-gmm-probability-density-diagnostic/evidence-doctor.md`: `Errors=0, Warnings=1, Suggestions=0` | attempted / positive diagnostic | Kept as Phase 020 sub-evidence; no direct production trigger. |
| GMM probability / terminal weights | synthetic image public-shaped wrapper | GMM `K=5`, float color, terminal `-log` | `probabilityDensity(c)` and `initGraph` terminal weights without graph mutation | `make run_test_compare` passed; terminal max budget ratio `0.004656613` | `bench_grabcut --case terminal_weights` | Milkv-Jupiter `640x480`, `iterations=8`, `warmup=2`, 5-run B/A `1.5433, 1.5398, 1.5348, 1.5250, 1.5485` | `computeTerminalWeightsCandidate` calls RVV per-Gaussian helper and has vector accumulation/store instructions | `doc/phases/020-terminal-weight-public-shaped-diagnostic/repeated-evidence-doctor.md`: `Errors=0, Warnings=0, Suggestions=0` | completed / positive diagnostic | Phase 030 已完成；该行作为 PI1 的 terminal formula 子证据保留。 |
| `initGraph` no-solve probe | synthetic indices / trimap public-shaped wrapper | GMM `K=5`, terminal weights + lightweight terminal write sink, no max-flow | test-support helper boundary for `initGraph` terminal source/sink work after terminal weights | `make run_test_compare` passed; `InitGraphNoSolveMatchesScalarReference` covers terminal costs and sink checksum | `bench_grabcut --case initgraph_no_solve` | Milkv-Jupiter `640x480`, `iterations=8`, `warmup=2`, 5-run B/A `1.5486, 1.5416, 1.5418, 1.5396, 1.5389` | `computeTerminalWeightsCandidate` calls `computeGMMProbabilityCandidate`; GMM symbol contains RVV load/store/FMA | `doc/phases/030-initgraph-no-solve-diagnostic/repeated-evidence-doctor.md`: `Errors=0, Warnings=0, Suggestions=0` | completed / partial-production-candidate | Superseded by Phase 060 production-detail evidence; kept as diagnostic input only. |
| PI1 production integration plan | production public entry / private `initGraph` detail | `Image<Color>` after `initCompute`; GMM K=5 float Color; organized and non-organized `indices_` stay semantically scalar outside terminal unknown batch | doc-only production scope plan | PI3 test plan executed in Phase 060: real `GrabCut<PointXYZRGB>::initGraph()` path and fixed-label fallback | PI4 production-detail bench executed | Phase 060 production-detail 5-run positive | production symbol attribution completed in Phase 060 | Phase 060 Doctor `Errors=0, Warnings=0, Suggestions=0` | completed / superseded by Phase 060 and user confirmation | Phase 070 public wall-time evidence completed. |
| production terminal-weight RVV helper | indices ordered unknown trimap | `PointXYZRGB` via `Image<Color>` / float GMM | `GrabCut<PointT>::initGraphTerminalWeightsRVV()` called from `initGraph()` after graph node allocation | `make run_test_compare` passed; Std 4/4, RVV 6/6; production direct and fixed-label fallback covered | `bench_grabcut --case production_initgraph_terminal` | Milkv-Jupiter `640x480`, `iterations=8`, `warmup=2`, 5-run B/A `3.1292, 3.1280, 3.0998, 3.0944, 3.1982` | `initGraphTerminalWeightsRVV` contains RVV load/store/FMA; `initGraph()` calls helper | `doc/phases/060-production-initgraph-terminal-evidence/repeated-evidence-doctor.md`: `Errors=0, Warnings=0, Suggestions=0` | adopted production behavior | Current production patch retained; formal `doc-rvv` closeout required and in progress. |
| public `extract()` wall-time after adoption | organized image grid with central background/unknown trimap | `PointXYZRGB` organized cloud / float color / public API | real `GrabCut<PointXYZRGB>::setBackgroundPointsIndices()` + `extract()` | QEMU log-shape smoke checksum matched; final correctness still covered by `make run_test_compare` | `bench_grabcut --case public_extract` | Milkv-Jupiter clean `96x72`, `iterations=3`, `warmup=1`, 5-run B/A `1.124687, 1.110452, 1.112645, 1.113482, 1.112866`, median `1.112866x`; 320x240 partial smoke was too heavy but direction positive | asm attribution inherited from Phase 060 production helper; public case proves dispatch inclusion, not new symbol family | `doc/phases/070-public-extract-wall-time-adoption-check/repeated-evidence-doctor.md`: `Errors=0, Warnings=0, Suggestions=0` | completed / production-public positive | Phase 080 profile and Phase 090/100 `learnGMMs()` diagnostics completed; current next step needs production integration authorization. |
| public `extract()` component profile | organized image grid with central background/unknown trimap | `PointXYZRGB` organized cloud / float color / public-shaped wrapper | test-only profile wrapper splits `setBackgroundPointsIndices()` + `extract()` components | QEMU log-shape smoke checksum matched; no production dispatch change | `bench_grabcut --case public_extract_profile` | Milkv-Jupiter clean `96x72`, `iterations=3`, `warmup=1`, 5-run B/A `1.169912, 1.181390, 1.167619, 1.166456, 1.160458`, median `1.167619x`; `learn_gmms` Std median `191.261680 ms`, `11.462780%` | inherits current production helper; profile case adds no new RVV production symbol | `doc/phases/080-public-extract-component-profile/repeated-evidence-doctor.md`: `Errors=0, Warnings=0, Suggestions=0` | completed / profile_actionable | Phase 090 assignment-only and Phase 100 full `learnGMMs()` diagnostics completed. |
| `learnGMMs` component assignment RVV | organized image grid with foreground/background mask | `PointXYZRGB`-like float `Color`, K=5 GMM | test-support helper for first loop of `learnGMMs()`; no GaussianFitter relearn | `GrabCutDiagnosticReference.LearnGMMComponentAssignmentMatchesScalarReference`; `make run_test_compare` passed with Std 5/5 and RVV 7/7 | `bench_grabcut --case learn_gmm_assignment` | Milkv-Jupiter clean `96x72`, `iterations=8`, `warmup=2`, 5-run B/A `3.564149, 3.511878, 3.528303, 3.613924, 3.522483`, median `3.528303x` | test-support helper boundary; production asm not applicable yet | `doc/phases/090-learn-gmm-component-assignment-diagnostic/repeated-evidence-doctor.md`: `Errors=0, Warnings=0, Suggestions=0` | completed / positive diagnostic | Superseded by Phase 100 full `learnGMMs()` production-shaped diagnostic. |
| full `learnGMMs` assignment plus relearn | organized image grid with hard segmentation | `PointXYZRGB`-like float `Color`, K=5 GMM | production-shaped diagnostic for scalar assignment + scalar relearn versus RVV assignment + scalar relearn | `GrabCutDiagnosticReference.LearnGMMsFullMatchesScalarReference`; `make run_test_compare` passed with Std 6/6 and RVV 8/8 | `bench_grabcut --case learn_gmms_full` | Milkv-Jupiter clean `96x72`, `iterations=8`, `warmup=2`, 5-run B/A `3.078492, 3.048585, 3.033853, 3.088773, 3.041691`, median `3.048585x`; Std median `4.078463 ms`, RVV median `1.337755 ms` | test-support helper boundary; production asm not applicable to this diagnostic | `doc/phases/100-learn-gmms-full-production-shaped-diagnostic/repeated-evidence-doctor.md`: `Errors=0, Warnings=0, Suggestions=0` | completed / superseded by Phase 110 production evidence | Phase 110 production integration loop completed to PI5. |
| production `learnGMMs` RVV assignment + scalar relearn | ordered `Indices` over `Image<Color>` | float `Color`, K=5 GMM | real `pcl::segmentation::grabcut::learnGMMs()` free function; RVV covers Step 4 assignment, GaussianFitter relearn remains scalar | `make run_test_compare` passed with Std 8/8 and RVV 10/10; production direct and small-input fallback covered | `bench_grabcut --case production_learn_gmms` plus public `bench_grabcut --case public_extract` | production-detail Milkv-Jupiter clean `96x72`, `iterations=8`, `warmup=2`, 5-run B/A `2.676933, 2.755010, 2.782530, 2.743741, 2.838160`, median `2.755010x`; production-public clean `96x72`, `iterations=3`, `warmup=1`, 5-run B/A `1.197422, 1.218683, 1.191227, 1.215751, 1.207907`, median `1.207907x` | `learnGMMsRVV` / `assignGMMComponentsForGroupRVV` in `bench_grabcut_rvv.full.asm` contain `vle32.v`, `vse32.v`, `vfmacc.vf`, `vfmacc.vv`; public case proves full `extract()` includes production path | production-detail `repeated-evidence-doctor.md` and production-public `public-repeated-evidence-doctor.md`: both `Errors=0, Warnings=0, Suggestions=0` | adopted production behavior | Current production patch retained after user confirmation. Phase 120 post-adoption profile completed and did not justify another helper. |
| post-adoption public component profile | organized image grid with central background/unknown trimap | `PointXYZRGB` organized cloud / float color / public API | test-only profile wrapper after `learnGMMs()` production adoption | `make run_test_compare` passed with Std 8/8 and RVV 10/10; QEMU smoke checksum matched | `bench_grabcut --case public_extract_profile` | Milkv-Jupiter clean `96x72`, `iterations=3`, `warmup=1`, 5-run B/A `1.232341, 1.290653, 1.321640, 1.293081, 1.284386`, median `1.290653x`; RVV `learn_gmms` median `57.534405 ms`, `4.592618%`; main residual is `initgraph_refine` + `graph_solve` | no new RVV symbol expected; profile only ranks residual components after current adopted helpers | `doc/phases/120-post-learn-gmms-adoption-profile/repeated-evidence-doctor.md`: `Errors=0, Warnings=0, Suggestions=0` | completed / profile_non_actionable | Stop current topic; reopen only with new profile or user scope proving a remaining component is a high-value RVV target. |
| color staging | public `initCompute` image conversion | `PointXYZRGB` / RGB or RGBA packed fields / organized and non-organized cloud | possible RGB/RGBA to `Image<Color>` conversion batch | not implemented | not implemented | no board evidence | no asm | not_applicable | deferred / profile required | Reopen only if profile shows color conversion is a meaningful public-entry cost and traits / fallback plan is authorized. |
| max-flow state machine | graph nodes / maps / deque | solver state | `BoykovKolmogorov::solve` | not_applicable with evidence | not_applicable | not_applicable | not_applicable | not_applicable | rejected with evidence | State-heavy active set and orphan adoption remain production scalar boundary in this topic. |
