# GrabCut 阶段索引

| phase | 状态 | 恢复入口 | 说明 |
| --- | --- | --- | --- |
| `000-current-state-and-diagnostic-plan` | completed / diagnostic evidence split | `000-current-state-and-diagnostic-plan/plan.zh.md` | 建立 organized n-link / color staging 的测试与 component bench（组件性能测试）诊断边界；上一批 all-case board 观察中 organized n-link 为弱负向。 |
| `010-gmm-probability-density-diagnostic` | completed / positive diagnostic | `010-gmm-probability-density-diagnostic/result.zh.md` | 覆盖 `segmentation/src/grabcut_segmentation.cpp` 中 `GMM::probabilityDensity(i, c)` 的 same-chain（同构链路）RVV 诊断，板卡 GMM component 单次为 `1.69x`，Evidence Doctor 为 `Errors=0, Warnings=1`。 |
| `020-terminal-weight-public-shaped-diagnostic` | completed / positive diagnostic | `020-terminal-weight-public-shaped-diagnostic/result.zh.md` | 覆盖 K=5 GMM mixture（混合概率）和 terminal `-log` 口径，Milkv-Jupiter 5-run repeated B/A 为 `1.5433, 1.5398, 1.5348, 1.5250, 1.5485`，Evidence Doctor 为 `Errors=0, Warnings=0`。 |
| `030-initgraph-no-solve-diagnostic` | completed / partial-production-candidate | `030-initgraph-no-solve-diagnostic/result.zh.md` | 在 terminal weight 之后追加 lightweight terminal write sink（轻量端点写入消耗点），Milkv-Jupiter 5-run repeated B/A 为 `1.5486, 1.5416, 1.5418, 1.5396, 1.5389`，Evidence Doctor 为 `Errors=0, Warnings=0`；仍不包含真实 graph mutation（图结构写入）或 max-flow（最大流）。 |
| `040-production-integration-plan` | completed / PI2 authorization boundary | `040-production-integration-plan/result.zh.md` | 将 Phase 030 的正向诊断证据转换为 PI1 production integration plan（生产接入计划），冻结候选范围、fallback（回退路径）、production direct tests（真实生产入口测试）和 PI2-PI5 暂停条件；未获授权前不修改 production 源码。 |
| `050-evidence-registry-target-alias` | completed / registry adopted | `050-evidence-registry-target-alias/result.zh.md` | 为 Phase 030 和后续 production evidence（生产证据）补 summary-only registry（只提交摘要的证据登记）入口。 |
| `060-production-initgraph-terminal-evidence` | completed / adopted after user confirmation | `060-production-initgraph-terminal-evidence/result.zh.md` | 验证当前 production patch：`initGraph()` unknown trimap terminal batch 真实写入 graph terminal capacities，Milkv-Jupiter 5-run B/A 为 `3.1292, 3.1280, 3.0998, 3.0944, 3.1982`，Evidence Doctor 为 `Errors=0, Warnings=0, Suggestions=0`；用户随后确认保留 / 采纳。 |
| `070-public-extract-wall-time-adoption-check` | completed / production-public positive | `070-public-extract-wall-time-adoption-check/result.zh.md` | 接入后补测真实 `setBackgroundPointsIndices()` + `extract()` public wall-time，Milkv-Jupiter clean 96x72 5-run B/A 为 `1.124687, 1.110452, 1.112645, 1.113482, 1.112866`，median `1.112866x`，Evidence Doctor 为 `Errors=0, Warnings=0, Suggestions=0`。 |
| `080-public-extract-component-profile` | completed / profile_actionable | `080-public-extract-component-profile/result.zh.md` | 接入后拆分 public-shaped profile（公开入口形态剖析），Milkv-Jupiter clean 96x72 5-run B/A median `1.167619x`，Evidence Doctor `0/0/0`；`learn_gmms` 在 Std 中约占 `11.46%`，是下一轮可尝试的公式型组件。 |
| `090-learn-gmm-component-assignment-diagnostic` | completed / assignment_positive | `090-learn-gmm-component-assignment-diagnostic/result.zh.md` | 新增 `learnGMMs()` assignment-only（仅分量归属）测试专用 RVV candidate，Milkv-Jupiter clean 96x72 5-run B/A median `3.528303x`，checksum 一致，Evidence Doctor `0/0/0`；该结果只支持继续完整 `learnGMMs()` 生产形态诊断。 |
| `100-learn-gmms-full-production-shaped-diagnostic` | completed / positive production-shaped diagnostic | `100-learn-gmms-full-production-shaped-diagnostic/result.zh.md` | 新增完整 `learnGMMs()` production-shaped diagnostic（生产形态诊断）：RVV assignment + scalar relearn 对比 scalar assignment + scalar relearn，Milkv-Jupiter clean 96x72 5-run B/A median `3.048585x`，checksum 一致，Evidence Doctor `0/0/0`；该结果支持提出有界 production integration loop（生产接入闭环），但不能直接作为已采用生产行为。 |
| `110-learn-gmms-production-integration-plan` | completed / adopted after user confirmation | `110-learn-gmms-production-integration-plan/result.zh.md` | 真实 `pcl::segmentation::grabcut::learnGMMs()` 已接入 RVV assignment（RVV 分量归属选择）生产路径。production-detail 5-run median `2.755010x`，production-public `public_extract` 5-run median `1.207907x`，两层 Evidence Doctor 均 `0/0/0`；用户确认按接入后板卡 positive 采纳。 |
| `120-post-learn-gmms-adoption-profile` | completed / profile_non_actionable | `120-post-learn-gmms-adoption-profile/result.zh.md` | 接入 `learnGMMs()` 后重跑 `public_extract_profile` component profile（组件剖析）：Milkv-Jupiter clean 96x72 5-run median B/A `1.290653x`，checksum 一致，Evidence Doctor `0/0/0`；RVV profile 中 `learn_gmms` 中位占比 `4.592618%`，低于继续优化阈值。 |
| `130-closeout-and-submit-audit` | completed / ready_for_commit | `130-closeout-and-submit-audit/result.zh.md` | 复核 Phase 120 停止条件、production dispatch / fallback、doc suite role inventory、长期 `doc-rvv` freshness、evidence registry 和 summary-only 提交边界；当前 topic 可以结束并进入提交。 |

当前默认恢复动作：当前 topic 已收尾并进入提交边界；无当前 topic 内高价值未阻塞优化方向。当前工作区保留两个 adopted production patch
（已采纳生产补丁）：`initGraph()` unknown terminal helper，以及 `learnGMMs()` RVV assignment helper。
Phase 120 证明剩余主要耗时为 `initgraph_refine` 和 `graph_solve`；GaussianFitter accumulation、
LMUL / ILP variants、n-link 和 color staging 当前都没有足够 profile 支持继续自动推进。
