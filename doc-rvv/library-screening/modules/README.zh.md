# 模块函数级清单目录说明

本目录用于存放按模块拆分的函数级筛查文档。

- 每个模块一个文件，命名规则：`<module>-function-triage.zh.md`。
- 文档固定三段：
  - 函数/函数族梳理
  - 函数级筛选评估标准说明
  - 文件级评估汇总表
  - 优先级与向量化候选总表
- 当前口径：阶段 A（全文件全函数粗筛）已覆盖 17 个中高优模块。
- 判断策略：全文件不漏，`impl/*.hpp` 与 `src/*` 作为主要实现依据，`include/*.h` 用于补充内联实现与语义上下文。
